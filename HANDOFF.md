# Handoff — Supercuts Analytics

Last updated: 2026-08-10. **Real bug found and fixed — bulk-imported Goals were silently getting clobbered.** Commit pending, pushed to `main`.
- **The report**: user asked why goals they'd imported had disappeared.
- **This session got real Supabase read access for the first time** — the user provided the project URL + a `sb_publishable_...` anon key, now in a local `.env` (git-ignored; added a `.gitignore` to this repo for the first time, covering `.env`/`node_modules`/`build` — none of that was ignored before, see `CLAUDE.md`). This means future sessions with the same `.env` present can query Supabase directly (read confirmed working via the REST API with this key; writes/dev-server login not yet exercised).
- **Confirmed via direct query**: `store_goals` currently holds real data for only **4 stores** (`8150`, `80577`, `80651`, `82993`), each with all four goal fields set. The Sales-Accrual data covers ~60 stores and the transcribed July goal sheets covered ~50 — so whatever larger import(s) the user ran never made it into Supabase. Not a display bug; the data was genuinely gone server-side. (Ruled out first: the 2026-08-06 `retailGoal`→`bottleGoal` rename leaving orphaned data — there's no `retailGoal` key anywhere in the stored payload, so that wasn't it.)
- **Root cause**: `handleImportGoals` (`src/App.js`) built its merged `next` goals object from the `goals` closure variable and saved it with a plain `await`, the same shape the Sales-Accrual/Attendance/Reviews batch importers used to have before they were fixed (see the "Live test in progress" section's sibling bugs, and the `historyRef`/`importChainRef` comment in `App.js` around the state declarations). The Goals tab shows **four import buttons side by side** (Sales/Color/Bottle/Signature Service) — clicking through them quickly (a natural workflow) let two or more imports read the same stale `goals` snapshot and race to save; whichever save round-trip finished last silently overwrote the others', dropping their store entries from Supabase even though the browser briefly showed them.
- **Fix**: same pattern already used for `historyRef`/`importChainRef` — added a `goalsRef` (kept in sync via `useEffect`) and queued `handleImportGoals` through the existing `importChainRef` so goal imports can never run/save concurrently; each one now always starts from the true latest state instead of a pre-import snapshot. `handleSaveGoal` (single-cell manual edits) already used a safe functional `setGoals(prev => ...)` update and didn't need this fix.
- **Verified**: real `npm run build`, clean. **Not verified**: an actual live re-import — next session (or the user, live) should redo the bulk Bottle/Signature Service/Sales/Color goal imports now that the race is fixed, and confirm the store count in `store_goals` afterward matches expectations (spot-check via the Supabase REST query pattern above, or just look at the Goals tab for all stores).
- **Also still open from 2026-08-06**, unrelated to this fix: the transcribed July goal sheets had an unresolved reconciliation gap (SS short by 49, Bottles over by 231 across ~50 rows) that the user was asked to spot-check against the physical sheets before uploading — worth asking whether that ever got resolved, since it'd affect the accuracy of a re-import even after this bug fix.

Last updated: 2026-08-08 (yet later still, same day). **Stores tab merged into Overview; tab bar reordered.** Commit `ed41094`, pushed to `main`.
- **Why**: Overview's Top 10/Bottom 10 leaderboards and Stores' full per-store list (with employee drill-down, goal tracking, manager tags) were two separate tabs built on the same underlying store data — user asked to combine them, with the store list living below the leaderboards.
- **`OverviewTab`** (`App.js`) now contains both: date range bar, search, the tap-a-metric tile grid + Top 10/Bottom 10 leaderboards (unchanged), then the full store list/company-footer that used to be the standalone `StoresTab` (now deleted, folded in). One shared `storeRows`/`filtered` computation feeds both sections instead of each tab recomputing its own — the leaderboards now also respond to the employee-aware search (a store shows up if an employee inside it matches, not just the store name), a small behavior change from Overview's old store-name-only search.
- **Tab bar reordered** to: Homepage, News, HSA, Overview, DL, Retail, Color Sales, Signature Service, Reviews, Employees, Weekly, Tillie's Nest, Goals, Setup (was: Homepage, News, HSA, Overview, Stores, Employees, Retail, Color Sales, Signature Service, Goals, DL, Reviews, Weekly, Tillie's Nest, Setup). Purely a `TABS` array reorder — role-based visibility filtering (`visibleTabs`) untouched.
- **Verified**: real `npm run build`, clean (bundle actually shrank ~150B, net code removed). **Not verified**: an actual live look — same standing limitation, user again declined pairing with the Claude Chrome extension this round. Worth confirming next session: does the merged Overview tab read well with the store list below the leaderboards, and does the new tab order feel right in practice.

Last updated: 2026-08-08 (yet later still, same day). **Real bug found and fixed — the MANAGER tag didn't appear after setting someone's role to Manager + a store code in Setup > Employee Access.** Commit `ba4e6bd`, pushed to `main`.
- **The bug**: the user reported that changing an employee's role to Manager and giving them a store code in Setup > Employee Access had no visible effect — no "MANAGER" tag next to their name in the DL tab.
- **Root cause**: two entirely separate systems that happen to share the word "manager." The visible tag (Stores/Employees/DL/Retail/Color Sales) is driven by `withManagerFlag()`, which reads a `managers` map (`{storeCode: managerName}`) maintained only via Setup > **Managers** — a hand-typed name per store, unrelated to login/role data. Setup > **Employee Access** (the real login roster, `role`/`storeCodes` columns) never wrote into that map at all. A manager had to be entered twice to look right.
- **Fix**: new `reconcileManagerTagsFromRoster(prevEmployees, nextEmployees)` (`App.js`, next to `handleSaveManager`) diffs the roster's before/after employee lists and writes into the same `managers` map (`store_managers` Supabase key) whenever an active employee's role is `manager` for a given store code — assigning on gain, clearing on loss (only if the map's current name for that store still matches the employee losing it, so a name someone typed directly into Setup > Managers for a non-roster person is never stomped). Wired into `EmployeeAccessSetupTab`'s existing `refresh()` (fires on initial load, every inline edit save, and every roster upload) via a new `onRosterChanged` prop, so **no new API call or schema change was needed** — this runs entirely off data the tab already fetches.
- **Self-heals on open**: because `refresh()` also runs once on mount, simply opening Setup > Employee Access reconciles any store that was already misconfigured this way before this fix existed — the user doesn't need to re-edit anyone who's already set up correctly in Employee Access.
- **Hint text updated** in both Setup > Employee Access and Setup > Managers to explain the auto-sync, so Setup > Managers now reads as "only needed for a manager who isn't a login-roster row, or to override the auto-assigned name."
- **Verified**: real `npm run build`, clean. **Not verified**: an actual live look (no local Supabase in this sandboxed session, standing limitation) — the user declined pairing this session with the Claude Chrome extension for live click-through testing this round. Next session should ask whether the tag now shows up correctly after opening Setup > Employee Access once, and whether editing a role/store-code inline still shows it immediately.

Last updated: 2026-08-08 (yet later same day). **Real bug found and fixed — emoji showing as "??????" in the actual sent leaderboard email; also added a Visit Site button.** Commit `32b67a3`, pushed to `main`.
- **The bug**: user sent a screenshot of the real Monday email — the section-header emoji (📣, 📅, 🛍️, 🎨, 💇) all rendered as strings of `�` diamond-question-marks, while the plain `·` separator next to each date rendered fine. The distinguishing factor: every broken character is astral-plane (above U+FFFF, needs a 4-byte UTF-8 / UTF-16 surrogate pair), the `·` is not. Something in the Apps Script `UrlFetchApp.fetch` → `GmailApp.sendEmail` round-trip was mangling the wider encoding, even though the JSON response itself is well-formed UTF-8 out of Vercel.
- **Fix**: replaced every emoji literal in `api/leaderboard-email.js` with numeric HTML character references (new `EMOJI` const, e.g. `&#x1F4E3;` for 📣) instead of the raw Unicode character. This makes the wire format pure ASCII — nothing left for any hop in the pipeline to misinterpret — and the email client's own HTML parser resolves the entity to the glyph at render time, same as `&amp;` already was for ampersands in this file.
- **New "Visit Site" button**, centered under the header banner's "Week of..." line, linking to the live site's homepage. URL is derived from `req.headers.host` at request time (not hardcoded, not an env var) — since this endpoint is only ever hit by calling the live deployment, the incoming request's own host header is always the real, current domain.
- **Verified**: `node --check`; real `npm run build`, clean; a synthetic-data render confirmed (a) no raw astral-plane character remains anywhere in the generated HTML string (regex-checked) and (b) the button renders with the right href. **Not verified**: an actual live send — the original bug report came from a real send screenshot, but there's no way to trigger another real send from this sandboxed session. **Next session should ask whether the emoji now render correctly in the next Monday email** — if they still don't, the mangling is happening somewhere other than the raw-Unicode-in-transit theory (e.g. inside Apps Script's own string handling of the fetched JSON), which would need a different fix.

Last updated: 2026-08-08 (later same day). **Leaderboard email redesigned visually, and News section now prioritizes 3 specific groups.** Commit `a7c1a90`, pushed to `main`.
- **Why**: user said the first pass (below) looked "pretty bland," and separately clarified the News section should surface **Stylist Info**, **Specials Offer**, and **Around the Corner** posts specifically — these are real Group values the user's team types into the News post form (Setup > Homepage), confirmed directly by the user, not something inferable from code since News groups are free text stored in Supabase.
- **Visual redesign** (`api/leaderboard-email.js`): whole email restyled around the site's own brand palette (`BRAND` const, mirrors `:root` in `src/App.css` — navy/gold/silver/bronze/green/red) instead of bare unstyled tables. Navy header banner ("Supercuts Analytics — Weekly Recap"), colored section-title bars, card-style News/Events entries (was plain table rows), numbered rank badges on the leaderboards with gold/silver/bronze medal coloring for the top 3, alternating row stripes, green/red value coloring for top vs. bottom performers. Still 100% inline-styled table layout, no `<style>` block/flexbox — has to survive being dropped into `GmailApp.sendEmail`'s `htmlBody` across whatever client actually opens it.
- **News section behavior changed twice this round**: first from "posted this week only" to "always show the latest 6 posts" (so the section is never empty on a quiet week) — then added `PRIORITY_GROUPS = ['Stylist Info', 'Specials Offer', 'Around the Corner']`, which now sort to the top of those 6 in that fixed order (each with its own fixed accent color via `PRIORITY_GROUP_ACCENTS`), with everything else still shown after, by recency. A gold "NEW" pill still flags anything posted during the leaderboard's own week window. **If the user ever renames or adds a group they want prioritized, this is a one-line edit** (the `PRIORITY_GROUPS`/`PRIORITY_GROUP_ACCENTS` consts near the top of `newsSection`).
- **Named export added**: `export { buildEmail }` alongside the default handler — purely so this could be exercised with synthetic data from a standalone script (no live Supabase in this sandbox). Harmless to leave in.
- **Verified**: `node --check` on every edit; real `npm run build`, clean. **Visually verified this round, unusually** — rendered `buildEmail()` against synthetic data (multiple stores, News posts covering all 3 priority groups plus an ungrouped one, upcoming events) via a throwaway script, published the actual HTML as a Claude Artifact, and the user reviewed and approved the rendered look before this was pushed — a stronger verification pass than this project's usual "compiles clean, never seen it render" caveat. **Still not verified**: an actual live Monday send with real Supabase data/real News posts — next session should ask how the next real send looked, especially whether `PRIORITY_GROUPS`' exact strings match what's actually being typed into the Group field in practice (a typo or trailing space would silently fall through to the generic accent/ordering instead of erroring).

Last updated: 2026-08-08. **Weekly leaderboard email now also recaps News posts and upcoming Events.** Commit `3e229e1`, pushed to `main`.
- **Why**: user confirmed the Monday leaderboard email (below) is live and asked whether it could also say what's new — reads the same `homepage_news`/`homepage_events` Supabase keys the Homepage tab already uses, so nothing needs to be maintained twice for the email to stay in sync with what's posted on the site.
- **News**: posts whose `date` falls within the same week window the leaderboard itself covers (`getLastFullWeekRange()`, i.e. since the last Monday email) show under a "📣 New This Week" section, most recent first. A quiet week with no posts just omits the section rather than showing an empty header.
- **Events**: upcoming events — filtered the same way `FeaturedEvents` (Homepage tab, `src/App.js`) does, `(endDate || date) >= today` — show under "📅 Upcoming Events", sorted soonest-first, capped at 8. Deliberately **not** scoped to "added this week": an event posted two weeks ago should keep showing as a reminder right up until it happens, not just in the one email right after it was added. Section omitted entirely when there's nothing upcoming.
- **Where in the email**: both new sections render above the existing Retail/Color/SS leaderboards, inside `buildEmail()` (`api/leaderboard-email.js`).
- **Verified**: `node --check` on the edited file; real `npm run build`, clean; a standalone Node script (not committed) re-ran the exact filter/sort logic against synthetic news/events data (in-range/out-of-range dates, an ongoing multi-day event, an event dated exactly today) and confirmed the boundary conditions land where intended. **Not verified**: an actual live send with real News/Events data — no local Supabase credentials in this sandboxed session, same standing limitation as everything else in this doc. Next session should ask whether the next Monday send actually included the right posts/events once the site has some real News/Events data to draw from.

Last updated: 2026-08-06 (yet later same day). **Weekly Retail/Color/SS Top 10-Bottom 10 leaderboard email.** Commit `a13e44d`, pushed to `main`.
- **What it does**: a Google Apps Script, on a Monday time trigger, calls a new `api/leaderboard-email.js` and emails the response's HTML (via `GmailApp.sendEmail`) to a recipient list the user maintains directly in the script — same free, self-deployed, no-new-hosting pattern as the existing report-upload automation, just running outbound instead of inbound. Computes the most recently completed Monday–Sunday week's per-store totals for Retail, Color Sales, and Signature Service (count + $), and returns Top 10 / Bottom 10 for each as one HTML email.
- **New `src/metrics.js`**: extracted `getRangeTotals`, `historyTotalsToReportShape`, `rollupRows`, `addRangeInto`, `expandDateRangeDays`, `mergeEmployeesInto`, `finalizeEmployee`, `addDaysISO`, `isoWeekStart`, `getLastFullWeekRange`, `sortByMetric` out of `App.js` (pure functions, no React) so this new serverless endpoint can compute the exact same numbers the live app shows, instead of a second hand-copied version of the weekly-vs-daily dedup rule that could quietly drift out of sync — this app has had real bugs from exactly that kind of duplication before (see the 2026-07-30 "stale-report"/Signature S sections further down). `App.js` now imports all of these from `./metrics` instead of defining them locally; verified with a standalone synthetic-data smoke test (not committed) that `getRangeTotals`/`sortByMetric`/`rollupRows` still produce correct per-store sums/ratios and rankings after the move.
- **New `api/leaderboard-email.js`**: secret-gated (`LEADERBOARD_EMAIL_SECRET`, same shared-secret pattern as `EMAIL_INGEST_SECRET`) — no user session involved, since the caller is Apps Script, not a logged-in browser. Loads `daily_history`/`weekly_history` (legacy single-row + chunked, same legacy-first/split-priority merge `App.js`'s load effect uses, minus that path's diagnostics/self-healing which don't belong in a stateless read), runs `getRangeTotals` for `getLastFullWeekRange()`, and builds the HTML server-side (inline-styled tables, Gmail-safe) so the Apps Script itself stays mechanical — fetch, then send.
- **Setup > Email Reports** gained a second card ("Weekly Leaderboard email (Mondays)") with its own script + 4-step walkthrough (add script → authorize → weekly Monday trigger → matching Vercel env var), deliberately a separate script/secret from the report-upload automation above it so the two can't interfere with each other.
- **Deliberately not filtered**: a store with zero data for the week still ranks (bottom, trivially) — matches the existing in-app `Leaderboard` component's behavior (Overview tab's Top 10/Bottom 10) exactly, for consistency, rather than silently excluding it.
- **Not built**: any UI for managing the recipient list — it's a plain array literal the user edits directly in the Apps Script, same as `SHARED_SECRET`/`WEBHOOK_URL` already are for the other automation. Also not built: any per-recipient scoping (e.g. a DL only seeing their own stores) — this is one company-wide email to everyone on the list, since that's what was asked for.
- **Verified**: real `npm run build`, clean; `node --check` on both new/edited files; a standalone Node smoke test of the `metrics.js` functions with synthetic multi-store data (not committed). **Live setup confirmed working end-to-end same day**: user deployed the script, added `LEADERBOARD_EMAIL_SECRET` to Vercel, redeployed, and a manual test run succeeded — real email delivered. One real snag along the way, for future reference: Firefox's Enhanced Tracking Protection blocked the Apps Script OAuth authorization redirect (`"Sorry, unable to open the file at this time"` on both the Run button and the Add Trigger flow) — switching to Chrome for the authorization step resolved it immediately. If this comes up again for HSA's or the report-upload automation's own Apps Script auth (same underlying Google OAuth flow), try Chrome first before troubleshooting further.

Last updated: 2026-08-06 (later same day). **Owner-only login audit log — who's logged in and how many times.** Commit `6350ff6`, pushed to `main`.
- **Why a new table, not just `sessions`**: `sessions` rows are deleted on logout (`api/auth.js`'s `logout` action) — it's a "who's currently signed in" table, not a history, so it can never answer "how many times has X logged in." New append-only `login_events` table (`employee_id`, `employee_name` snapshot, `created_at`) gets one row inserted on every successful `login` and `signup` in `api/auth.js` via a new `logLoginEvent()` helper — best-effort (wrapped in try/catch, failure doesn't block the actual login).
- **New Supabase table — SQL below, not yet run by the user as of this handoff** (same "hand SQL to the user" pattern as `employees`/`sessions`/the points tables). RLS enabled with no policies, same posture as those tables — only reachable via the service-role key.
  ```sql
  create table login_events (
    id bigserial primary key,
    employee_id bigint not null,
    employee_name text not null,
    created_at timestamptz not null default now()
  );
  alter table login_events enable row level security;
  ```
- **New `loginCounts` action in `api/roster.js`** — owner-gated by the same top-of-handler role check every other roster action already uses. Fetches all `login_events` rows and rolls them up into per-person `{ employeeId, employeeName, count, lastLogin }` in JS (no SQL GROUP BY) — same pragmatic-over-precise style as the O(n) PIN scan in `serverAuth.js`, fine at this app's login volume. `employeeName` reflects whichever name was current as of that person's most recent login, in case they're later renamed.
- **UI**: new "🔐 Login Activity" section in Setup > Employee Access (`EmployeeAccessSetupTab`), below the existing roster table — a "Show Login Activity" button (loads on demand, not automatically, since it's not core to that tab) reveals a Name / Times Logged In / Last Login table, sorted by count descending. This tab is already restricted to the owner role at the top level (`visibleTabs` in `App.js` only shows the whole Setup tab to `role === 'owner'`), so no additional client-side role check was needed here — the server-side `loginCounts` gate in `api/roster.js` is the real enforcement either way.
- **Verified**: real `npm run build`, clean; `node --check` on both edited serverless functions. **Not verified**: an actual live login/signup actually writing a row, or the Login Activity table rendering with real data — no local Supabase credentials in this checkout, same standing limitation as everything else in this doc. **Next session: ask whether the `login_events` table SQL above has been run** — nothing will show up in Login Activity until it exists (a login/signup will still succeed either way, since the insert is best-effort and swallows its own error).

Last updated: 2026-08-06. **Retail goal swapped to a bottle count, Signature Service goal fixed to a service count (not $), and the 60 Day Employee tab folded into Employees.** Commits `71cd446`, `12db026`, `f64e23e`, `abd48a3`, pushed to `main`.
- **Retail's goal is now a bottle (unit) count, not a dollar figure** — the user's real DL goal sheets track retail as "how many bottles," not "$ retail." New `bottles` field threads through the whole Sales-Accrual pipeline the same way `signatureS` already does (Product-type line-item qty, summed store/company-wide, patched into report-covered days since the live Stylist Report has no item-level detail to double-count against). `goals[code].retailGoal` renamed to `bottleGoal` (feature was brand new this session, no real data existed yet, so a straight rename was safe). Wired into Tilly's context.
- **`StoreMetricTab` gained `goalMetricKey`/`goalMetricLabel`/`goalFmt` props** so a tab's Goal/vs Goal columns can track a different unit than its headline metric — Retail still displays $ retail + RPC as before, but Goal/vs Goal now compares actual bottles vs. `bottleGoal`, formatted as a plain count. Color and Signature Service are unaffected by default (goal still tracks the same $ metric shown) — **except**, per direct user correction, Signature Service's goal is *also* a count, not $: fixed by pointing its `goalMetricKey` at the already-existing `signatureSCount` field (shown in its own "SS Count" column) with an integer formatter, instead of the dollar `signatureS` field. Tilly's context and the Goals tab's hint text/placeholders were updated to spell out which of the four goal types are $ vs. unit counts, since Signature Service dollar totals appear elsewhere in the same Tilly context and could otherwise look contradictory.
- **Auto-transcribed the user's handwritten July goal sheets (3 photos)** into two ready-to-import `.xlsx` files (DL | Store | Goal format) for Signature Service and Bottle goals, generated directly into the user's own Downloads folder via a Node script using the same `xlsx` library the app already depends on. Cross-checked the transcription against the sheet's own printed grand-total (4249 Signature Services / 3136 Bottles) — first pass didn't reconcile (a large error traced to one outlier store, Exeter, confirmed and fixed by the user), and even after that fix there's still a real unresolved gap (SS short by 49, Bottles over by 231) somewhere across ~50 other rows. **Not fully resolved as of this handoff** — the user was given the full transcribed table with every uncertain read flagged (a few tape-covered cells, a couple of names inferred from ambiguous handwriting) and asked to spot-check against the physical sheets before uploading; next session should ask whether that reconciliation happened and whether the goals were actually uploaded.
- **60 Day Employee tab removed, folded into Employees.** First cut made "New Hires (last 60 days)" an entry inside the existing sort dropdown, but the user asked for it as its own toggle button instead (since it's a different view, not really a sort order) — now a standalone button next to the sort dropdown; toggling it on swaps the table to the old tab's start-date/DL/days-since-hire columns (with its own secondary sort dropdown), independent of whatever metric sort is selected. Reuses the same `buildNewHireRows` logic verbatim, just relocated. **One real behavior change**: the standalone tab used to render even with zero report and zero historical data (roster-only). Now that it's inside Employees, it sits behind the same `needsReport` gate as the rest of that tab, so it needs *some* report or historical data to exist first. Not expected to matter given how much historical data already flows through this app, but a fresh install with only an employee roster and nothing else would no longer show new hires until some sales data exists too.
- **Verified**: real `npm run build`, clean, after every change this round. **Not verified**: an actual live look at any of this — no headless browser or local Supabase in this sandboxed session, same standing limitation as everything else in this doc. Worth checking live: does the Retail tab's new "Bottles" column and vs-Goal read sensibly, does Signature Service's vs-Goal now show a plausible count instead of a dollar-looking number, and does the Employees tab's new "New Hires" button render/toggle correctly with real data.

Last updated: 2026-08-05 (yet later same day). **Reviews date quick-filters, Sales/Signature Service goal import, and a downloadable blank goal template.** Commit `c09803e`, pushed to `main`.
- **Reviews tab's date range bar** — added "Current Month" and an always-visible "Show All Reviews" button (was previously a "Clear" link that only appeared once a range was already picked). Mirrors the quick-action buttons `DateRangeBar` already has elsewhere in the app.
- **Goals tab now has Import buttons for Sales and Signature Service goals**, alongside the existing Color/Retail ones — `parseGoalFile` (`src/parser.js`) was already generic by column position (DL | Store | Goal, ignoring header text), so this was just two more buttons wired to the same import path. Fixed the toast's goal-type label along the way (was a hardcoded `field === 'colorGoal' ? 'color' : 'retail'` ternary that would've mislabeled the two new fields — now a proper `GOAL_FIELD_LABELS` map).
- **New "Download blank goal sheet" button** on the Goals tab — generates a real `.xlsx` (`downloadGoalTemplate` in `parser.js`, using the `xlsx` library already in the project for reading) with columns DL | Store | Goal, one row per store the logged-in user can see, grouped by DL. The DL column is cosmetic only (so whoever fills it in can see which stores are theirs) — the importer never reads it. One template works for all four goal types since import matches purely by column position, not header text.
- **Verified**: real `npm run build`, clean, after each change. **Not verified**: an actual live download/fill-in/re-upload round trip, or how the new buttons look on a real screen — no headless browser or local Supabase in this sandboxed session, same standing limitation as everything else in this doc. Worth the user trying the full loop once: download the template, put a number in a couple of Goal cells, upload it back through each of the four import buttons, and confirm the right goal type actually changed for the right store.

Last updated: 2026-08-05 (later same day). **New Signature Service tab, a DL-facing Goals tab, and a tab-bar restyle.** Commit `45fa000`, pushed to `main`.
- **Signature Service tab** — a new top-level tab, built on the same `StoreMetricTab` component Retail/Color Sales already use (DL-grouped/flat views, employee drill-down, goal tracking). `signatureS`/`signatureSCount` were already rolled up everywhere in the aggregation pipeline (company/group/store/employee), so this was pure UI wiring, no new data plumbing.
- **Goals moved out of Setup into its own top-level "Goals" tab**, visible to everyone except plain Employee role (was previously owner-only, buried in Setup). The point: a District Leader can now enter their own stores' goals directly — `report.stores` is already filtered server-side per role (`api/scoped-data.js`), so a DL naturally only ever sees/edits their own stores through this UI, same trust model as every other write in this app (see the 2026-07-28 entry on the anon-key write model). Extended the existing Color/Retail goal fields with **Sales Goal** and **Signature Service Goal** columns (all stored in the same `store_goals` per-code object, just two new field names: `salesGoal`, `signatureSGoal`). Also stopped hard-requiring a live Stylist Report to list stores — it now falls back to the Sales-Accrual/Attendance-derived store list (`fallbackEmployeesByStore`), consistent with how the rest of the app has worked since Stylist Report became optional.
- **Sales Goal display**: unlike Color/Retail/Signature Service (which got a full "Goal"/"vs Goal" table column pair for free via `StoreMetricTab`), the Stores tab isn't built on that shared component — it's a flat card-based layout with no per-metric goal columns architecture. Added a lighter-weight "Sales Goal"/"vs Goal" stat pair to each store's card header (only shown once a goal is set for that store) rather than a deeper Stores-tab rework. **Worth asking the user** if they want a fuller Sales-goal treatment (e.g. in the company-wide footer table) — this was a deliberate lighter touch, not full parity.
- **Tilly's context** (`buildAIContext`) updated to include all four goal fields (was Color/Retail only) — per this project's standing rule to wire new fields into Tilly as part of the same change.
- **Tab bar restyled**: was a single-row underlined-tab strip that would have needed horizontal scrolling with 16 tabs now (up from 14). Changed to wrapping, bordered chip buttons (`.tab-nav`/`.tab-btn` in `App.css`) — active tab is a filled navy chip, matching the color language already used for `.view-toggle-btn`.
- **Verified**: real `npm run build`, clean. **Not verified**: an actual live look — no headless browser/local Supabase in this sandboxed session, same standing limitation as everything else in this doc. Specifically worth checking next session: does the new tab bar wrap sensibly on a phone-width screen (this was reasoned through CSS, not visually confirmed), does a District Leader's own login actually show the Goals tab with only their stores in it, and does a saved Signature Service goal actually show its "vs Goal" column correctly once real data exists for a store.

Last updated: 2026-08-05. **Real bug found and fixed — Mullica Hill was missing from Reviews (By DL and flat list both), not a data gap after all.** The 8/3 entry below concluded Mullica Hill was correctly configured (code `80354`, grouped under Jennifer Sutton) and just hadn't had an upload with data yet — true for Sales-Accrual, but the user checked the actual Reviews export and found it identifies this store as `80654`, not `80354`. Every review row for that store was silently keyed to a code with no matching entry in `STORE_CODE_TO_NAME`, so `ReviewsTab`'s `storeMap` (built only from codes present in the review data) never created a row for it at all — not even an "unrecognized code" fallback entry, since `resolveStoreName` only produces one of those when a review row exists in the first place. Fixed with a new `canonicalizeStoreCode()` (`src/storeDirectory.js`) and a `CODE_ALIASES` table (`{ '80654': '80354' }`, commented with why) — applied in `parseReviewsFromGrid` (`src/parser.js`) so every future reviews upload self-corrects at parse time. Commit `eec6e57`, pushed to `main`. **Verified**: real `npm run build`, clean. **Not verified**: a live upload — Setup > Upload's reviews slot fully replaces the list each time (per the standing note elsewhere in this doc), so the *next* normal reviews upload should make Mullica Hill appear under Jennifer Sutton automatically; worth confirming next session. Already-stored review rows from before this fix still carry the wrong `80654` code until the next full reviews upload overwrites them — no backfill script was written since a normal upload naturally supersedes it.

Last updated: 2026-08-03. Ten pieces of work landed today (git log has the commit-by-commit history; this is the consolidated summary):

- **Real brand assets** replaced hand-drawn placeholder art: header (white wordmark), login screen (blue wordmark + robin badge), Tilly's chat FAB (real G&C robin badge instead of a generic bird SVG). Old placeholder SVG components deleted.
- **Homepage Top 10 Leaderboards (Employees mode)**: store name now appended next to a name whenever the same person could appear twice (e.g. a District Leader who also shows up as a stylist at more than one of their own stores).
- **Sunday review-email automation (Rallio, via Gmail)**: fixed a silent-failure bug — the Apps Script only handled emails with a real attachment, and this Rallio email is a link-only "click to download" with none. Now falls back to pulling a csv/xlsx/xls link out of the email body and fetching it directly when there's no attachment. User redid the whole Gmail setup under the correct account and confirmed it's live; the actual first successful run is still unconfirmed — ask next time.
- **New HSA (Hair Stylist Academy) tab**: class schedule upload (Setup > HSA) merges into the existing Homepage events calendar (tagged `source: 'hsa'`, deterministic ids so re-uploads don't orphan sign-ups); public sign-up with Name + Phone + Store (DL auto-derived from store, not typed) — phone is owner-only in the visible roster (real PII, unlike name/store/DL which stay visible to everyone); classes groupable "By Date" or "By Class Type"; optional Google Sheets export via a self-deployed Apps Script (`HSA_SHEET_WEBHOOK_URL`/`HSA_SHEET_SECRET` **not yet set** in Vercel). **User still needs to upload the real `classdates2026.xlsx`** — it was only used to build/test the parser, never actually loaded onto the live site.
- **Homepage calendar**: enlarged, then fixed to a uniform cell height (was `min-height`, letting busier weeks stretch taller than lighter ones — now a fixed height + `overflow:hidden`, up to 3 pills/day). Hovering a pill now shows the full title + location/description, not a repeat of the truncated label.
- **Retail tab gained a "Products" view**: Sales-Accrual rows with Item Type "Product" now have their actual Item Name tracked (qty + $), not just lumped into a generic Retail total — Top 10 company-wide, expandable per DL group, expandable per store. **Only forward-looking** — files already uploaded before this change never had item names retained, so historical months show nothing here unless those original exports are re-uploaded.
- **Default date range changed from week-to-date to month-to-date everywhere** (Overview/Stores/Employees/Retail/Color Sales/DL/Tilly/Homepage Top 10) so opening a tab never requires picking a range first. `getCurrentWeekRange()` was removed (now unused).
- **Weekly tab**: metric picker moved to the top as big clickable tiles (was a small dropdown, same `.summary-tile` style Overview already uses); Signature Service now shows `count|$amount` like every other tab instead of a bare dollar figure.
- **Real bug fixed — Reviews tab "By DL" view was showing no review text.** CSS inheritance issue: that view nests a review list inside a `<td>` governed by `white-space: nowrap; text-align: right` (correct for that table's numeric columns), which cascaded into the review text with nothing to stop it, collapsing it onto one long line pushed off-screen instead of wrapping. Fixed by resetting both properties on `.review-list`.
- **Store directory**: "Valley View" renamed to "Naamans" (canonical name flipped; old name kept as an alias for the Stylist Report export, which may still use it). Mullica Hill investigated and confirmed already correctly configured (code `80354`, user confirmed this matches their real files, already grouped under Jennifer Sutton) — it's just never had an upload with data for that store yet, a data gap not a bug.

**Verified across all of today's work**: real `npm run build`, clean, after every change; several pieces had extra targeted verification (a standalone Node test for the HSA schedule parser against the real file, a standalone test for the products-pipeline aggregation/store-isolation logic, `node --check` on the extracted Apps Script text after each edit that touched it). **Not verified for any of it**: an actual live look in a browser — this sandboxed session has no headless browser and no local Supabase credentials, the same standing limitation noted throughout this doc.

**Open items for next session**:
- Has the real HSA class schedule been uploaded (Setup > HSA)?
- Did the Sunday Rallio review email get picked up cleanly by the Gmail automation (check for a "Supercuts report upload failed — SC-Reviews" email, or a fresh timestamp on Setup > Upload's reviews slot)?
- Has Mullica Hill shown up correctly under Jennifer Sutton in Reviews after the next reviews upload (see 2026-08-05 fix above)? Also worth checking whether the same `80654`-vs-`80354` mismatch exists in any *other* upload source (Sales-Accrual/Attendance/roster) for this store, or if Reviews was the only one using a divergent code.
- Does everything from today actually look right live — calendar sizing, the Reviews-tab fix, the Products view, the Weekly tab tiles, the HSA sign-up flow?
- If the Google Sheets export for HSA sign-ups is still wanted, walk through the Setup > HSA walkthrough to set up `HSA_SHEET_WEBHOOK_URL`/`HSA_SHEET_SECRET`.

Last updated: 2026-08-02. **New this session**: real Supercuts/G&C brand assets added, replacing hand-drawn placeholder art. The user provided the actual brand-book PNGs (`wordmark.png` — white combo lockup of the G&C robin badge + "SUPERCUTS" wordmark + tagline, for dark backgrounds; `Supercuts_logo.png` — dark-navy "SUPERCUTS" wordmark only, for light backgrounds; `G&C_LightLogo.png` — the full-color G&C robin badge icon) from `~/Downloads`, now copied into `src/assets/branding/` as `supercuts-wordmark-white.png`/`supercuts-wordmark-blue.png`/`gc-robin-badge.png` and imported into `App.js`. Three placements: (1) **main app header** — the old hand-drawn `HeaderLogo` scissors-SVG and the "SUPERCUTS" `<h1>` text are both gone, replaced by the white wordmark image (already includes the badge + wordmark + tagline in one lockup); (2) **login screen** — the blue wordmark now sits atop the login card on every step, and the color robin badge shows on the initial "choice" screen above "🔒 Supercuts Metrics"; (3) **Tilly's chat FAB** — the old hand-drawn `RobinNestIcon` SVG (a generic bird silhouette) is replaced by the real G&C robin badge image, which is a nice fit since Tilly was already themed as a robin/nest mascot (🪺) before any real asset existed for it. Both now-dead SVG components (`HeaderLogo`, `RobinNestIcon`) were deleted rather than left unused. **Verified**: real `npm run build`, clean, no new warnings. **Not verified**: an actual live-rendered look — no headless browser or Chromium automation available in this sandboxed session (same category of limitation as the "no local Supabase" one elsewhere in this doc, just for rendering instead of data). Sizing was chosen by reasoning through each asset's real pixel aspect ratio (checked via a PNG-header read, not guessed) against its CSS container, but worth the user pulling up the actual site once deployed to confirm nothing looks oversized/cramped, especially the login card on a narrow phone screen.

Last updated: 2026-08-01. **New this session**: three asks — (1) verified the 2026-07-31 Reviews-backfill logic by code review (no live Supabase access this session either), see "What shipped this session — Reviews backfill, code-reviewed" below; (2) changed the login system so "Create a Login" checks phone number alone instead of employee code + phone, and gave the owner a way to set/change any employee's PIN directly from Setup > Employee Access, see "What shipped this session — phone-only signup, owner-settable PINs"; (3) header title simplified to "SUPERCUTS", and the "Company (weighted)" footer row on Stores/Retail/Color Sales is now owner-only (new `isOwner` prop) — for a DL/Manager it was really just a total across their own already-scoped stores mislabeled as "Company", since scoping happens server-side before the client ever sees the data. Commits `01fef1f`, `1477d50`, `46b7793`, all pushed to `main`. **Verified**: real `npm run build`, clean, after each change. **Not verified**: an actual live signup, Set PIN click-through, or non-owner-role view of Stores/Retail/Color Sales — no local Supabase credentials in this sandboxed checkout, same standing limitation as every other change in this doc.

## What shipped this session — Reviews backfill, code-reviewed (2026-08-01)
User asked to double-check the 2026-07-31 Historical Import reviews backfill (commit `acb4661`) is importing correctly. No live Supabase access this session either, so this was a careful read-through of `handleImportReviewsBatch` (`src/App.js`) rather than a live test:
- **Merge/dedupe logic is correct**: within a batch, `existingKeys`/`newOnes` are recomputed from `working` after each file, so file 2 in the same multi-select correctly dedupes against file 1, not just against what was already saved before the batch started. The null-initial case (no reviews ever uploaded before) is handled correctly. Uses the same `reviewKey(r)` (`code|postedAt|userName|rating`) as every other consumer of reviews, so identity stays consistent app-wide. Queued through `importChainRef`, same as Sales-Accrual/Attendance.
- **One minor, pre-existing-shape cosmetic issue found, not fixed** (wasn't asked, flagging for awareness): the top-level `reviews.fileName` field — shown as the "last file uploaded" status line on the ongoing Setup > Upload reviews slot — gets set to `working?.fileName || parsed.fileName` during a historical batch import, i.e. it keeps whichever file was already on record rather than updating to reflect the batch. After running a multi-month historical backfill, that status line under Setup > Upload could show a stale/unrelated filename. Harmless (doesn't affect what reviews are stored or displayed anywhere else), but could be confusing if it comes up. Easy fix later if it bothers the user: update it to the last file in the batch, or just drop the filename display for the batch path.
- **`postedAt` sort relies on the field being ISO-like/lexically sortable** (`YYYY-MM-DD`-ish) — same assumption every other date sort in this codebase already makes (`localeCompare`, `.slice(0,10)`), not something this feature introduced or changed.

## What shipped this session — phone-only signup, owner-settable PINs (2026-08-01)
Two related asks: drop employee code from "Create a Login" (phone number only), and let the owner see/manage everyone's PINs from Setup > Employee Access.
- **Two decisions confirmed with the user before implementing** (both change security/UX behavior, not just code): (1) PINs stay one-way bcrypt-hashed, same as before — the owner does **not** get literal visibility into an employee's existing PIN (that would require storing PINs in reversible/plaintext form, which the user explicitly did not want given this is the only thing gating access to store sales data). Instead the owner can set/overwrite anyone's PIN to a value of their choosing, anytime. (2) Phone numbers aren't unique in this roster (documented in the "Still open" section further down — Katie Ingram, Megan Friday, etc. share phones with other real people) — the user chose to **block** signup when a phone matches more than one still-unregistered account, telling that person to contact the owner, rather than showing a pick-your-name disambiguation step.
- **`api/auth.js`**: `findEligibleSignupRow(supabase, phone)` replaces the old code+phone lookup — queries by phone + active only, then filters to unregistered (`!pin_hash`) rows. Throws a user-facing error (with an HTTP status attached via `Object.assign`) for zero matches, an already-registered match, or more than one unregistered match; the route handler's catch block now respects `err.status` instead of always returning 500. `checkEligible`/`signup` actions both take `phone` only now (no `employeeCode`).
- **`api/roster.js`**: new `setPin` action (owner-only, same auth gate as `resetPin`/`update`) — validates the PIN against the same `PIN_PATTERN` used at signup, checks it's not already in use by someone else via `findEmployeeByPin` (skipping a self-match against the same employee id), hashes it with the existing `hashPin`, and writes `pin_hash` directly. This is additive — `resetPin` (clear the hash, person picks their own new one via Create a Login) is unchanged and still there.
- **`src/auth.js`**: `checkEligible`/`signUp` wrappers drop `employeeCode`; new `rosterSetPin(token, id, pin)` wrapper.
- **`LoginScreen`** (`src/App.js`): "Create a Login" step 1 is now just a phone number field (employee code input and its state removed).
- **Setup > Employee Access** (`EmployeeAccessSetupTab`, `src/App.js`): new **Set PIN** / **Change PIN** button per row (label depends on `e.registered`), next to the existing **Reset PIN**. Uses `window.prompt` for the PIN value (first use of `window.prompt` in this codebase — everywhere else uses inline fields or `window.confirm`; kept minimal rather than building new modal chrome for one field) — client-validates the pattern before calling the API, shows the same `msg` toast-area as everything else in this tab. Added a `section-hint` explaining the new phone-only signup behavior and what to do about a shared-phone collision.
- **Not done / out of scope**: no UI change to the roster upload columns (Employee Code stays a real, still-used admin/roster field — just no longer required at signup time). No self-service PIN recovery beyond what already existed.

Last updated: 2026-07-31. **New this session**: Setup > Historical Import gained a third upload slot, **Reviews Files** — the user has ~3 months of review-export files to backfill in one shot, and the existing single-file reviews upload (Setup > Upload) fully *replaces* the reviews list on every upload, which breaks for a multi-file backfill (each new file would wipe the previous one). The new slot instead **merges**: parses each file with the same `parseReviews`, dedupes against what's already on file via the existing `reviewKey(r)` (`code|postedAt|userName|rating`), and saves to the same `reviews` Supabase key the rest of the app already reads (Reviews tab, Homepage Shoutouts, Tilly). Multi-file select (`multiple`), sequential per-file processing, queued through the same `importChainRef` the Sales-Accrual/Attendance batches use. New summary tiles (review count, date range) added to the Historical Import tab, and its hint text was reworded to describe all three upload types. Commit `acb4661`. **Verified**: real `npm run build`, clean, node v24/npm 11 available this session. **Not verified**: an actual live upload of the user's real 3-month files — no local Supabase credentials in this sandboxed checkout, same standing limitation as every other change in this doc. **One thing worth flagging to the user if it comes up**: the ongoing single-file Setup > Upload reviews slot still fully replaces the list on every upload — that's fine as long as future normal exports keep including full history (the "Google-reviews style export" the original parser comment describes), but if a future normal upload ever only covers a recent window, it would silently wipe out the just-backfilled historical months. Worth keeping in mind, not changed this round since it wasn't part of the ask.

Last updated 2026-07-30 (previous session). New that session, in order: (1) DL tab Managers view now shows each manager's own individual numbers instead of their whole store's; (2) Reviews tab got a "Pin List" view for quick pin-handout reference; (3) a real bug behind Signature S "not updating" was found and fixed; (4) **the weekly Stylist Report upload is no longer required** — Overview/Stores/Employees/Retail/Color Sales/DL now all fall back to week-to-date Sales-Accrual+Attendance historical data when there's no live report (see "What shipped this session — Stylist Report is now optional" below, the most architecturally significant change this round). **The user has decided to stop uploading the Stylist Report starting now (2026-07-30) as a live test of that fallback** — see "Live test in progress" right below the Stylist-Report-optional section for what to watch for and what to do if something looks off. **That live test immediately surfaced a real bug in the fallback itself, since fixed** — see "Live test, round 1 — the stale-report bug" for the full story; short version: `report` never expires on its own, so a live report from the last real upload kept "winning" over the historical fallback indefinitely, and since that live format never carried Signature S, every tab showed SS stuck at 0 even with good historical data underneath. Fixed with a 10-day staleness check (`isReportStale`). **A handful of non-metric spots still read `report` directly and would have kept going stale the same way — also fixed same day**, see "Live test, round 2 — the remaining stale-report spots" right after round 1's section. Everything from the 2026-07-28 session below (points/rewards, login system, roster upload) is unchanged and still applies — see "Still open" further down for what's still pending from that round (the 3 points/rewards SQL statements, and the DL/Manager scoping verification pass). **Later the same day, a separate round of smaller UI asks landed** (Employee Access search, a Reviews date filter, two Homepage Top 10 toggles, and a real privacy blur for 1:1s) — see "What shipped this session — UI/UX round" right below this paragraph.

## What shipped this session — Reviews added to Historical Import, for a one-time multi-month backfill (2026-07-31)
The user has about 3 months of review-export files to upload as a one-time historical backfill and asked for it to live in Historical Import rather than the existing Setup > Upload reviews slot.

- **Why it couldn't just use the existing slot**: `handleReviewsFile` (Setup > Upload) treats every upload as the full, current reviews list and calls `setReviews`/`saveData('reviews', parsed)` outright — correct for the ongoing workflow (a Google-reviews-style export that's presumably a fresh full snapshot each time), but wrong for a backfill spread across several month-limited files, since file 2 would wipe out file 1.
- **New third upload slot in `HistoricalImportTab`**: "Reviews Files" (`multiple`, `.xlsx/.xls/.csv`, same as the Sales-Accrual/Attendance slots next to it). New `handleImportReviewsBatch` (`src/App.js`, next to `handleImportSalesBatch`/`handleImportAttendanceBatch`) parses each file with the existing `parseReviews`, then **merges** into whatever's already in `reviews` — deduping via the same `reviewKey(r)` (`code|postedAt|userName|rating`) already used everywhere else a review needs a stable identity (notes, gold combs, the Pin List) — so re-uploading a file, or two monthly exports that happen to overlap by a few days, never double-counts. Saves back to the same `reviews` Supabase key everything else already reads (Reviews tab, Homepage Shoutouts, Tilly's context) — this is additive to that data, not a parallel store. Queued through the existing `importChainRef` (same serialization the Sales-Accrual/Attendance batches already use) via a new `reviewsRef`, so a reviews import started right after one of those can't read a stale snapshot mid-save.
- **New summary tiles** (review count, date range) shown in Historical Import once reviews exist; hint text at the top of the tab reworded to describe all three upload types and the dedup behavior.
- **Verified**: real `npm run build` (node v24.18/npm 11 available this session), clean. **Not verified**: an actual live upload of the user's real files — no local Supabase credentials in this sandbox, same standing limitation noted throughout this doc. Next session should ask whether the backfill went smoothly and whether the review counts/date range shown in Historical Import looked right.
- **Worth flagging to the user, not changed this round**: the ongoing single-file Setup > Upload reviews slot still fully *replaces* the list on every upload. That's harmless as long as future normal exports keep covering full history the way they apparently have been — but if a future export ever only covers a recent window, it would silently wipe out the months just backfilled here. Not touched since it wasn't part of the ask, but worth a sentence of warning if the user starts using this new slot for more than a one-time backfill.

## What shipped this session — UI/UX round: search, date filters, Top 10 toggles, and a real privacy blur for 1:1s (2026-07-30)
Five small, independent asks in a row, each committed and pushed separately (5 commits, `d4f6fa3` through `6a632e8`):
- **Setup > Employee Access**: added a `SearchBox` (filters by name/phone/employee code/store code) and widened the Name field's `min-width` to 220px so a full name doesn't get clipped inside the edit input on narrower screens.
- **Reviews tab**: new `ReviewDateRangeBar` (own local `reviewDateRange` state, separate from the sales-side `DateRangeBar`/`dateRange` used elsewhere) filters `allReviews` by `postedAt` before every total/store-card/category-count/pin-list is computed — so a date window scopes the whole tab, not just one view.
- **Homepage Top 10 Leaderboards**: two new toggles. **Stores/Employees** switches all four charts between per-store and per-employee rankings (`employeeRows` built the same way `EmployeesTab`'s historical fallback already flattens `report.allEmployees`/`getRangeTotals`). **Past 7 Days/Month to Date** picks which historical window feeds the charts when there's no live report — new `getLastFullWeekRange()` (a real completed Mon–Sun week, unlike the existing week-to-date `getCurrentWeekRange()`) vs. the existing `getCurrentMonthRange()`. Both toggles are local to `HomepageTab`. Note: a District Leader automatically only ever sees their own stores/employees in this widget, in both modes — `report`/`history` are already role-scoped server-side (`api/scoped-data.js`) before `HomepageTab` ever sees them, so no extra gating was needed.
- **Employee focus/privacy mode, upgraded and extended**: the existing "click a row to spotlight one employee" (previously Employees tab only, and only dimmed/grayscaled everyone else — still readable) now actually **blurs** non-focused rows (`filter: blur(5px)`, softening slightly on hover so the presenter can still glance) via updated `.ledger-table--focus-mode` CSS in `App.css`. The same click-to-focus `EmployeeTable` props (`focused`/`onFocus`, already existed but unused by these callers) are now wired into **Stores tab**, **Retail/Color Sales** (both "Grouped by DL" and "All Stores" views), and **DL tab's Full Stats view** — previously only `EmployeesTab` had this. The "Store total / weighted avg" footer row (`tfoot`) is now blurred too in a follow-up fix — it was still fully visible after the first blur pass, which defeated the point since it's built from everyone's numbers, not just the focused person's.
- **Deliberately not wired up**: DL tab's **Managers** view (a hand-coded table, one row per manager, not the shared `EmployeeTable`) and the **60 Day Employee** tab (also hand-coded) don't have the focus/blur click yet — different table shape, would need the same treatment built by hand if wanted.

## What shipped this session — Stylist Report is now optional (2026-07-30)
User asked whether a real Sales-Accrual export (`Sales-Accrual(366).xlsx`, checked directly with the project's own `xlsx` parser) had enough to make the weekly Stylist Report upload redundant. It confirmed: 60 stores, a full month of dates, 298 stylists, zero blank `Stylist`/`Sold By` rows — exactly the columns `parseSalesAccrualFile` already expects. Sales/Color/Retail/CPC/RPC/Haircuts/Signature S are all derivable per employee per store per **day** this way (finer-grained than the Stylist Report's once-a-week number). The only real gap was Hours (for TSTH/CPH) — already covered by the existing Attendance History import, which merges into the same `history` records. The handful of Stylist-Report-only fields (`prodHours`, `nonProdHours`, `daysWorked`, `otherNet`, `opc`) are parsed but confirmed **unused anywhere** in the UI or Tilly's context — not a real blocker.

The actual blocker was architectural, not data: `needsReport` (`App.js`) hard-gated Overview/Stores/Employees/Retail/Color Sales/DL on a live `report` object existing at all, even though Stores/Retail/Color/DL already had a working historical (Sales-Accrual+Attendance-fed) code path as an optional overlay. Fixed by making that path the *default* instead of report-gated:

- **New `getCurrentWeekRange()`** (`App.js`, near `getCurrentMonthRange`) — week-to-date (Monday through today) via the existing `isoWeekStart`. This is the implicit "current period" once there's no live report to anchor on.
- **`needsReport` gate loosened**: now only blocks when there's neither a live `report` **nor** any `history`/`weeklyHistory` data at all (`hasHistoricalData` check). The empty-state copy was updated to mention both upload paths.
- **StoresTab, StoreMetricTab (Retail/Color), DLTab**: each already computed `isHistorical` from the `dateRange` prop and branched to `getRangeTotals` vs. the live `report`. Changed to `usingDefaultRange = !report && !(dateRange.start && dateRange.end)` → `effectiveRange = usingDefaultRange ? getCurrentWeekRange() : dateRange`, so `isHistorical` is now always true when there's no live report (even with no explicit date picked), instead of crashing on `report.stores` being null. The `DateRangeBar` itself still shows the user's raw (blank-by-default) picker state — a `section-hint` line explains the week-to-date default underneath it when it's in effect, so it doesn't look like a picker silently applied a range on their behalf.
- **OverviewTab and EmployeesTab had NO historical support at all before this** — 100% dependent on `report`. Both gained `history`/`weeklyHistory`/`dateRange`/`onDateRangeChange` props (threaded from the same App-level state Stores/DL already share) and the identical `usingDefaultRange`/`effectiveRange` pattern. Overview's store rows and company totals, and Employees' flattened `allEmployees` list, are now built from `getRangeTotals` the same way StoresTab already did — `report.allEmployees`' shape (`{ ...employeeFields, store: storeName }`) is reproduced by flattening each store's `shape.employees` in the historical path.
- **Verified with a standalone logic harness** (not committed, ran in a scratch file), since there's no way to log in with real Supabase data from this sandboxed session: fed `getRangeTotals`/`historyTotalsToReportShape`/`rollupRows` synthetic daily history with **zero `weeklyHistory` entries at all** (the actual no-Stylist-Report scenario) and confirmed correct per-store and per-employee sums/ratios, plus re-confirmed the Signature S covered-day fix still holds when a legacy weekly report *is* present. Also ran a real `npm run build` (Node/npm turned out to be available this session, see `CLAUDE.md`) — compiled clean both before and after this change.
- **Not verified**: an actual live click-through with real data/login, since there's no `.env`/Supabase credentials in this local checkout. Worth the user exercising Overview/Stores/Employees/Retail/Color Sales/DL live with the Stylist Report upload paused, to confirm the fallback looks right in practice (especially: does week-to-date feel like the right default vs., say, last full week?).
- **Not changed**: the Setup tab's own report-dependent sub-forms (e.g. the goals-setup store list, which still needs a live `report` to list stores) — out of scope, a separate admin-only guard untouched by this session.

## Live test in progress — Stylist Report uploads paused (started 2026-07-30)
User's plan, in their own words: "stop uploading stylists and let's see what breaks or if nothing then we are golden." So as of 2026-07-30 the weekly Stylist Report is **not** being uploaded — Sales-Accrual + Attendance are the only inputs. If this session ends before the user reports back, a fresh session should ask how it went before assuming either outcome.

**What to check if the user reports something looks wrong**, roughly in order of likelihood given what's untested (no live click-through happened yet, see above):
- **TSTH/CPH reading blank or wrong** — means Attendance isn't actually being uploaded on a matching cadence to Sales-Accrual (hours has to come from somewhere now; nothing derives it). Ask whether Attendance History imports are actually happening regularly, not just Sales-Accrual.
- **Week-to-date numbers looking "too low" early in the week** — expected, not a bug: Monday's numbers alone will look sparse compared to a full completed week. Confirm the user understands the new default is a running week-to-date total, not a completed-week snapshot like the old Stylist Report was. If they'd rather default to "last full week" instead, that's a real, easy change to `getCurrentWeekRange()` (App.js) — just ask which they prefer.
- **A store or employee missing entirely** — check the Sales-Accrual file's Center Name → store code resolution (`getCodeForStoreName`, same lookup the Master Salon List roster parsing already depends on) and whether that store/employee actually has any rows in the date range being viewed.
- **Milestone Goal thermometer (DL tab) looking off** — unrelated to this change, already historical-only (`monthToDateSalesByCode`) before this session; if it's wrong, that's a different bug, not a regression from this round.
- **Something in Setup** (goals list, employee access, etc.) **breaking** — those sub-forms were deliberately NOT touched this session (see "Not changed" above); if one of them needs a live report and now doesn't have one, that's a real gap this round didn't cover, not something already fixed.

If nothing breaks after a normal week of this, the user considers it validated ("golden") and the Stylist Report upload can be considered fully retired.

## Live test, round 1 — the stale-report bug (found and fixed same day, 2026-07-30)
Within hours of pausing Stylist Report uploads, the user reported "SS is not updating" / "showing 0 under SS" in every tab. Debugged live via the user's own browser console (the `[history load]` diagnostics already in the code, plus a new temporary one added mid-session) rather than guessing:

- **Confirmed the underlying data was fine**: 25,779 of 33,134 history records had nonzero Signature S after the user's Sales-Accrual upload, most recent dated 2026-07-29 (the last day that file covered) — inside the app's week-to-date default window (2026-07-27 to 2026-07-30, today). So the "Stylist Report is now optional" fallback's data pipeline itself was never the problem.
- **Actual root cause**: the fallback logic added earlier this session only activated when `report` was `null` (`usingDefaultRange = !report && ...`) — but `report` is Supabase-persisted state that **never expires on its own**. It's simply whatever Stylist Report was last uploaded, however long ago, until something new overwrites it. Since the user's last real upload was still sitting there, every one of the 6 fallback-aware tabs kept treating it as "the current report" and never switched over to the historical data at all — even though that live-report object *never had a Signature S field to begin with* (the Stylist Report export has no item-level detail, so `parseStylistReport`/`rollup()` never populated it). Every other field on that stale report still had real-looking (if increasingly outdated) numbers, which is exactly why only SS looked broken and nothing else did.
- **Fix**: new `isReportStale(report)` (`App.js`, next to `getCurrentWeekRange`) — a report counts as usable only if it exists AND its own `endDateISO` is within `REPORT_STALE_AFTER_DAYS` (10) of today. All 5 tabs' `usingDefaultRange` checks changed from `!report` to `isReportStale(report)`, so once the last real upload ages past ~10 days, they automatically switch to the historical fallback instead of showing an ever-more-stale snapshot forever. The `section-hint` copy shown in that state was reworded from "No live weekly report on file" to "No current weekly report on file" since it's no longer strictly about the report's existence, just its freshness.
- **Why 10 days**: a real weekly report is only ever meant to represent about a week; 10 gives a few days' grace for a slightly late upload without waiting a full extra cycle. Easy to tune if it turns out wrong in practice — it's the one constant (`REPORT_STALE_AFTER_DAYS`) at the top of `isReportStale`.
- **Temporary diagnostic left in `App.js`'s history-load block** (safe to leave, same "cheap and inert" reasoning as the existing `[history load]` logs): logs how many history records have the `signatureS` field at all vs. how many are actually nonzero, plus a sample of the 5 most recent nonzero ones. Directly answered "is the data missing, zero, or just not being looked at" without needing SQL access. Worth keeping around for the next time something like this comes up, or removing once the live test settles down if the console noise becomes annoying.
- **Verified**: real `npm run build`, clean. Not re-verified with a live click-through (still no local Supabase credentials) — this was debugged entirely through the user's own browser console output relayed back into the session, which worked well as a pattern; worth reusing if another "X isn't updating" report comes in before there's a way to test locally with real data.

## Live test, round 2 — the remaining stale-report spots (same day, 2026-07-30)
After round 1's fix landed, the user asked directly: "so all the data on the metric side populates from just the sales accrual and attendance uploads correct?" That prompted an audit of every remaining direct `report.*` read in `App.js` — round 1 only touched the 6 metric tabs' `usingDefaultRange` checks, but several non-metric spots read `report` directly with no staleness awareness at all and would have kept going stale forever the same way:

- **New shared helper**: `getEmployeesByStoreFromHistory(history, weeklyHistory)` (`App.js`, next to `isReportStale`) — same `{code: [...employees]}` shape as `report.stores[].employees`, sourced from a trailing `EMPLOYEE_ROSTER_LOOKBACK_DAYS` (90-day) window via `getRangeTotals` rather than "this week only", since the point is "who works here at all" (name-matching, store lookup), not a financial total — a wider window catches someone who just didn't happen to have a sale in the current week. Computed once in the main `App` component as `fallbackEmployeesByStore` (a `useMemo`, placed before the `!currentUser`/`loading` early returns so the Rules of Hooks aren't violated) and threaded as a prop everywhere it's needed, rather than each consumer recomputing it.
- **Reviews tab** (`employeesForCode`) and **Homepage's "🌟 Shoutouts" widget** (`ReviewSpotlightWidget`) — both matched review text against employee names via `report.stores.find(...).employees` with zero staleness check. Now prefer the live report while `!isReportStale(report)`, otherwise use `fallbackEmployeesByStore`.
- **Homepage's "🏆 Top 10 Leaderboards"** — a spot round 1 missed entirely (the earlier grep for stale-report reads used a pattern that didn't catch `report.stores.map`). Gated purely on `report` truthiness, with "Upload a stylist report to see the Top 10 leaderboards" as its empty state. Now builds `storeRows` from the same week-to-date historical fallback the 6 metric tabs use once the report is stale, and shows "Data current through `<date>`" (per the user's explicit choice on how to phrase this) instead of the old messaging.
- **60 Day Employee tab** (`buildNewHireRows`) — used `report.allEmployees`/`report.stores.find` to show which store/DL a new hire currently belongs to, plus their sales/hours metrics. Now builds the same name → {store, metrics} map from `fallbackEmployeesByStore` when the report is stale, and uses `getCodeForStoreName` (already used elsewhere, e.g. `EmployeesTab`) instead of `report.stores.find` to resolve a store name to its code — works identically regardless of which source fed the roster.
- **Tilly's "CURRENT REPORT PERIOD" section** (`buildAIContext`) — previously printed "No current stylist report is loaded on the site right now" whenever `report` was absent, even with perfectly good historical data sitting right there. Restructured around new `currentStoreRows`/`currentAllEmployees` locals populated from whichever source is live — the live report's `stores`/`allEmployees` when fresh, otherwise `getCurrentWeekRange()` + `getRangeTotals()` (identical to the 6 tabs) — so the section always has real content, and the Employee Start Dates cross-reference right after it (store/DL enrichment) reuses those same locals instead of a second `report`-only lookup.
- **Deliberately NOT touched, correctly**: Setup tab's admin sub-forms (goals/managers/milestone lists — still explicitly excluded, per round-1 notes) and the Setup > Upload tab's per-slot "last file uploaded" status line (`fileInfo` on the Stylist Report `UploadSlot`) — that one was misidentified as a "Homepage" element earlier in the conversation before the code was actually checked; it's really just "what did you last upload here," which stays true and doesn't need to change.
- **Verified**: real `npm run build`, clean, after every edit in this round. Not re-verified live (same standing limitation — no local Supabase credentials).

## What shipped this session — Signature S fix, SS rename, DL Managers view, Reviews Pin List (2026-07-30)
Four asks in one session: two feature requests (DL Managers view, Reviews Pin List), one real bug fix (Signature S), and a rename that piggybacked on the bug fix since both touched the same field.

- **Root cause found for "Signature S not updating"**: `getRangeTotals` and `buildWeeklySnapshots` (`src/App.js`) both fully skip a day's daily history record whenever that date is already covered by an uploaded weekly Stylist Report — this is the existing, correct dedup rule that stops the two sources double-counting sales/hours/etc. But `buildWeeklyRecord` (`src/parser.js`) never captured Signature S at all (the routine Stylist Report has no item-level detail, so it can't). Net effect: any week that had **both** a routine weekly upload **and** a Sales-Accrual import — the normal case, every week, per the user's own stated workflow — had its Signature S silently zeroed out, because the weekly source "won" for that date and contributed nothing for that one field. Fixed in both functions: a covered day now still pulls `signatureS`/`signatureSCount` from the daily Sales-Accrual record (store- and employee-level), since the weekly source's contribution to those two fields is always zero — no double-count risk, unlike every other field. `buildWeeklySnapshots` also had to switch to cloning `weeklyHistory`'s per-week `stores` objects before patching them, so the fix doesn't mutate React state in place.
- **New `signatureSCount` field**, threaded everywhere `signatureS` already flowed: `parseSalesAccrualFile`/`mergeSalesIntoHistory` (`src/parser.js`), `EMPTY_RANGE_TOTALS`/`addRangeInto`/`mergeEmployeesInto`/`historyTotalsToReportShape`/`rollupRows`/`EMPTY_TOTALS`/`addInto` (`src/App.js`). Counts item quantity the same way `haircuts` already does (via the file's `Qty` column), not just $ amount.
- **Renamed "Signature S" → "SS" everywhere**, now displayed as `count|$amount` (e.g. `8|$412`) via a new `fmtSS(count, amount)` helper — DL tab (both Full Stats and Managers views), Stores/Employees tabs (`STORE_METRICS`/`EMPLOYEE_METRICS`), Overview tab summary tiles, Leaderboards, and Tilly's monthly context lines. **Scope decision**: the Weekly tab's year-over-year comparison table (`WEEKLY_METRICS`) keeps `SS` as a $-only figure — that pipeline only ever carries a single scalar per period (current/previous/diff), and plumbing a count through it for a "diff" cell didn't seem worth the added complexity without being asked; flagged to the user, not yet revisited.
- **DL tab → 👔 Managers view now shows the manager's own individual numbers**, not their whole store's rollup. Previously every column in that view (Sales, TSTH, Color, etc.) was just the store's totals with the manager's name in an adjacent column — looked like "the manager's stats" but wasn't. Now each row looks up that store's manager name in `s.employees` (same name-matching `withManagerFlag` already used elsewhere) and renders their individual row; if no employee row matches the manager's name for that period (name mismatch, or no report data yet), the row shows an explicit "No individual data found for `<name>` in this period" note instead of misleading zeros. The leader-group summary card header and the table's footer row are unchanged (still whole-store rollups) — those are DL/leader-level summaries, a different level of aggregation than "this one manager's numbers," and weren't part of the ask.
- **Reviews tab → new "Pin List" view**, for handing out one pin per review mention without reading through every review card. Only appears once "Only reviews mentioning an employee by name" is already on; toggling that off hides and resets it. Shows a flat list of stores, each with every mentioned employee and their mention count (respects whatever category/sentiment filter is also active, same as the rest of the tab) — built as a new `pinListRows` memo (`ReviewsTab`, `src/App.js`) rather than reusing the existing per-store `matchCount` (which is a single store-wide total, not broken out per employee).


The "points + shop" half of the rewards system discussed-but-not-built on 2026-07-26 (see that section further down) — the login/identity piece it needed is already built, so this only needed the points/shop layer. Not built this round: Twilio texting on shoutout (not asked for).

- **Click-to-award, wherever an employee's name shows.** Owner-only: clicking a name pops it into a button with a small "+5" hint; one click awards 5 points instantly (no confirm dialog — see below for how a mis-click gets corrected). Wired into `EmployeeTable` (`src/App.js`) — the single shared component behind Employees, Stores, Retail, Color Sales, and DL tabs — plus the 60 Day Employee tab's own hand-coded table, `ReviewCard`'s detected-mention tag (Reviews tab), and the Homepage `ReviewSpotlightWidget` shoutout name. **Not done**: the Homepage Top 10 leaderboards render via chart.js canvas bars, not DOM text, so they weren't wired in — a real "not done," not an oversight, since making a canvas bar clickable is a different mechanism (`getElementsAtEventForMode`) from the button-wrap used everywhere else.
- **Points are keyed by the plain employee NAME STRING**, not a roster foreign key — deliberate: most places a name renders come from parsed report data (`e.name`) with no guaranteed link to the `employees` login-roster row, same "exact-normalized-name, not fuzzy" limitation already true of Manager matching elsewhere in this app. Keying on name avoids a new silent-mismatch failure mode.
- **New `api/points.js`** (mirrors `api/roster.js`'s action-dispatch shape): `balance` (own balance + recent activity, any logged-in user), `award` (owner-only, +5, logs who awarded it), `allBalances`/`transactions` (owner-only admin views), `deleteTransaction` (owner-only — removes a transaction and reverses its delta; **this is the correction path for a mis-click**, not an undo-toast timer), `redeem` (spends the caller's own points — `employeeName` always comes from the session, never client-supplied, so nobody can redeem on someone else's behalf), `listRewards`/`saveReward`/`deleteReward` (catalog CRUD, owner-only to write), `markFulfilled` (owner-only, toggles a redemption's fulfilled flag). Balance updates are a plain read-then-upsert in the handler, not a stored procedure — this app has no real write concurrency to speak of (a single owner clicking a button), matching its existing pragmatic style (e.g. `serverAuth.js`'s O(n) PIN scan).
- **New Supabase tables — SQL below, not yet run by the user as of this handoff** (same "hand SQL to the user" pattern as `employees`/`sessions`): `employee_points` (running balance per name), `points_transactions` (append-only ledger — award/redeem/adjustment, `reward_name` snapshotted so history stays readable even if a reward is later renamed/deleted), `points_rewards` (owner-managed catalog — `stock` nullable means unlimited). RLS enabled with **no policies** on all three — locks the public anon key out entirely, only reachable via the service-role key, same posture as `employees`/`sessions`.
  ```sql
  create table employee_points (
    employee_name text primary key,
    balance integer not null default 0,
    updated_at timestamptz not null default now()
  );
  alter table employee_points enable row level security;

  create table points_transactions (
    id bigserial primary key,
    employee_name text not null,
    delta integer not null,
    type text not null check (type in ('award','redeem','adjustment')),
    reward_id bigint,
    reward_name text,
    awarded_by text,
    fulfilled boolean not null default false,
    created_at timestamptz not null default now()
  );
  alter table points_transactions enable row level security;

  create table points_rewards (
    id bigserial primary key,
    name text not null,
    description text,
    cost integer not null,
    stock integer,
    active boolean not null default true,
    created_at timestamptz not null default now()
  );
  alter table points_rewards enable row level security;
  ```
- **New "Tillie's Nest" tab**, visible to every logged-in role (unlike Setup, which stays owner-only) — everyone needs to reach it to spend their own points. `TilliesNestTab` in `App.js` is **just the shop**: "Your Points" balance + recent activity, a shop grid of active rewards with a `window.confirm`-gated Redeem button (disabled with a reason if the balance's too low or it's out of stock). (First cut of this session had the owner-only admin sections living in this same tab; moved out to Setup per direct user feedback — Tillie's Nest should read as "the shop," nothing else.)
- **Owner-only reward admin lives in Setup > Rewards** (`RewardsSetupTab`, new `SETUP_SECTIONS` entry) instead: **Manage Rewards** (inline add/edit/delete catalog list), **Redemption Queue** (pending redemptions with a "Mark fulfilled ✓" button — the to-do list of what to physically hand out), and **Everyone's Balances** (every `employee_points` row, doubling as an implicit leaderboard, each expandable to that person's transactions with a "✕ remove" to correct a mis-click). Same API/data underneath (`api/points.js` — nothing changed there), just relocated in the UI, same pattern as every other owner-only admin section (Goals, Managers, Employee Access, etc.) living in Setup.
- **Tilly wiring**: `buildAIContext` (`src/App.js`) gained a `points` param — the logged-in user's own balance always, plus (owner only) total points currently outstanding company-wide, top 3 balances, and count of pending unfulfilled redemptions. Fetched in a new dedicated `useEffect` in `App.js` (separate from the big central `Promise.all` load block, since points goes through `api/points.js` not `loadScoped`/`db.js`).
- **Not confirmed as of this handoff**: whether the user has run the 3 `CREATE TABLE` statements above. **Next session: ask, and if not, walk through running them** (same as the roster-upload SQL earlier this session) — nothing in this feature will actually persist until they exist. Once confirmed, worth a quick smoke test: award a real person 5 points from the Employees tab, confirm it shows in Tillie's Nest, add a cheap test reward and redeem it, confirm the redemption shows in the owner's queue.

## Housekeeping — Waxing the City project set up as a sibling folder (2026-07-27)
Not Supercuts work. The user has a second, unrelated project (a "Waxing the City" site, same stack: React/Vercel/Supabase) that a different Claude session had been building via the Claude app. This session helped move that work over to Claude Code:
- Its GitHub repo is `evanrobins6789-cyber/store-analytics` (repo/package name is a leftover generic template name — `"time-and-till"` — not renamed to reflect the Waxing the City branding; don't be thrown by that mismatch if it comes up again).
- Cloned locally to `C:\Users\evanr\Documents\waxing-the-city-analytics`, a sibling of this repo's folder.
- User is running it as a fully separate Claude Code session (separate terminal tab, separate `claude` process) rather than inside this Supercuts session/context — intentional, so the two projects don't share conversation memory or state. Still needs: real `.env` (Supabase URL/anon key — currently only placeholders in `.env.example`), `npm install`, and its own `CLAUDE.md`/handoff doc, all to be done in that session, not this one.

## What shipped this session — Setup-wide password gate, roster upload tooling, and a self-lockout bug fix (2026-07-28)
Started as a small ask (move the Goals password to the whole Setup tab) and grew into finishing out the employee roster upload from last session, including a new way to source it entirely.

- **Setup tab password gate**: the old `GOALS_PASSWORD` gate on just the Goals sub-tab is gone; instead the whole Setup tab (`SetupTab` in `App.js`) is gated behind one password (`SETUP_PASSWORD`, currently `'sc4310'`), re-locking every time you navigate away from Setup and back (same per-visit behavior the old Goals gate had, since `SetupTab` unmounts when `tab !== 'Setup'`). This is unrelated to and stacks on top of the real employee-login system — Setup is still only reachable by an owner-role login in the first place; this is an extra gate on top.
- **`EmployeeAccessRoster.csv` (on the user's machine, not in this repo) was fully hand-completed** this session — the 18 rows missing Employee Code/Phone from last session were filled in by cross-referencing `C:\Users\evanr\Downloads\Master Salon List July 2026.xlsx` (a 25-tab internal company workbook the user provided), plus the duplicate Malvern manager row was deleted (confirmed same person as the Area Supervisor). This CSV still exists and still works as an upload, but is likely superseded now by the next item — the user switched to uploading the Master List directly instead.
- **`parseEmployeeAccessFromGrid` (`src/parser.js`) no longer drops a row for missing Employee Code/Phone** — that person still gets added (with a stable per-name `PENDING-<slug>` placeholder code, generated in `api/roster.js`) so they show up in Setup > Employee Access instead of vanishing silently. A new `api/roster.js` `'update'` action (client wrapper: `rosterUpdate` in `src/auth.js`) lets the owner edit name/phone/code/role/store-codes **inline, per row, right in that table** — no need to re-upload the whole file to fix one person. Rows missing a code/phone show a "⚠ needs code/phone" flag.
- **New: upload the Master Salon List workbook directly**, instead of hand-building a CSV. `parseMasterSalonListFile`/`parseMasterSalonListFromGrids` (`src/parser.js`) reads three specific tabs of that workbook in one pass (`readWorkbookSheetsByName`, since the existing `readWorkbookGrid` only ever looks at the first sheet) and combines them into the same row shape the CSV path produces:
  - **"Emplopyee List"** tab (sic — that's the real tab name, a typo in the user's own file) → every stylist, `role: 'employee'`, using its real numeric Emp ID + phone. Salon name → store code goes through `getCodeForStoreName` (`src/storeDirectory.js`), which gained two new aliases this session (`"Morrell Plaza"` → 80684, `"Sinking Springs"` → 80014, spelling variants unique to this workbook). Rows whose salon doesn't resolve are skipped and reported, not guessed — this is what correctly filters out ~15 rows that turned out to be **Waxing the City** staff (the user's other business shares this same workbook, with salons literally named "WTC Concord/Media/Pike Creek").
  - **"Exec Team, DL & Salons"** tab → District Leaders and Area Supervisors (both map to this app's single `district_leader` role, matching `leaderRoster.js`'s existing convention), parsed via `parseLeaderGroupsGrid` — a named row followed by blank-name continuation rows, each contributing one more store code to that leader's group.
  - **"DLs and Managers"** tab → each store's Manager (`parseMasterManagersGrid`), same leader-group shape but with the Manager's name/email/phone per store row. A store is skipped from the Manager pass when that store's manager email matches the covering DL/Area Supervisor's own email — i.e. a self-managed store, already covered by the leader's own row. This is how "Christina Nole" (Area Supervisor) and "Christine Noles" (listed separately as Malvern's manager, same email different spelling) resolve to one person automatically, generalizing the same fix made by hand in the CSV.
  - **Admin Team and Education Team sections are deliberately never read** — the owner explicitly said to skip them (not stylists/DLs/managers), and this is also why the seeded Owner account isn't sourced from anywhere in this parse (see the bug below).
  - Found and fixed one real data bug while testing against the actual file (via a hand-written Perl simulation of the parsing logic, since there's no Node/browser available in this sandboxed session to run the real JS): the workbook lists "Allie Clifford" twice under slightly different names ("Allie Clifford" and "Allie Clifford (under KI)"), each with a different subset of her 4 stores. The overlay logic in `parseMasterSalonListFromGrids` now **merges** store codes into an existing matched row instead of overwriting it, and keeps the first-seen name — otherwise the second entry silently dropped a store from her coverage.
- **Bug found and fixed after a real (if brief) production incident**: uploading the Master Salon List deactivated the user's own seeded Owner account mid-upload. Cause: a roster upload replaces the full list and deactivates anyone whose employee code isn't in the new file — and the Master List parser deliberately never includes the Owner (it's not sourced from the Admin Team). The user got `"Your access has been removed. Contact the owner."` (from `requireSession` in `src/serverAuth.js`) and had no in-app way back in. Fixed in `api/roster.js`: the `'upload'` action's deactivation filter now always excludes `employee.id` (the account performing the upload), so **whoever uploads a roster file can never lock themselves out that way, regardless of whether they're in the file.** Immediate recovery for the live incident was a direct SQL `UPDATE employees SET active = true WHERE role = 'owner';` in Supabase — worth knowing this recovery path exists if a similar lockout ever happens again for a different reason.
- **Two schema changes handed to the user as raw SQL to run themselves** (this session has no direct Supabase access — Supabase changes always have to be relayed as SQL for the user to paste into their SQL Editor): `ALTER TABLE employees DROP CONSTRAINT employees_phone_key;` (three real people in the source data genuinely share a phone with another real person — same-household or multiple POS profiles — and the user confirmed "let them enter any option," so phone no longer needs to be globally unique; `checkEligible`/`signup` in `api/auth.js` already match on employee_code **and** phone together, never phone alone, so nothing relies on that uniqueness) and `ALTER TABLE employees ALTER COLUMN phone DROP NOT NULL;` (some rows — e.g. a stylist with no phone on file, or a DL/manager the master list has no phone for at all — legitimately have no phone yet, same "fill in later" pattern as a missing code). **Not confirmed as of this handoff whether the user has run both and successfully completed an upload** — check current `employees` row count / roster state before assuming this is done.

## What this app is
A Store Scoreboard / analytics dashboard for a Supercuts franchise (React SPA, Supabase-backed, deployed on Vercel). Tabs: **Homepage** (default landing tab), **News**, Overview, Stores, Employees, Retail, Color Sales, DL, 60 Day Employee, Reviews, Weekly, and Setup (which also holds Goals / Managers / Milestone Goals / Homepage (News & Events admin) / Historical Import / Upload / **Email Reports** as sub-sections). Has an in-app AI assistant, "Tilly," that answers questions about the business data via a serverless Anthropic API proxy (`api/chat.js`).

## What shipped this session — employee login system
User asked for real login (District Leaders see only their store group, Managers only their store, Owner sees everything) plus an admin-managed roster that can revoke access. This is genuinely new infrastructure — every prior "password gate" in this app (`GOALS_PASSWORD`, the Setup tab's joke banner) was a client-side flag that reset on reload, not real auth. Full design reasoning is in the plan file the session used: `curried-booping-stonebraker.md` (Claude Code plan mode), worth reading if picking this back up since it explains the tradeoffs, not just the "what."

**Scope decision the user explicitly made**: protect only the sensitive data (sales numbers, goals, reviews) with real server-side enforcement this round — not a full lockdown of every read/write in the app. Rationale: the Supabase anon key is already public (compiled into the site's JS bundle, like any React app) and nothing currently restricts what it can read/write; truly closing that off means rewriting how every single tab loads *and saves* data (homepage news, goal edits, review notes, etc.), which is a much bigger and riskier job than "add login" alone. So: homepage news/events, the employee start-date list, review notes/gold combs, and **all writes app-wide** still go through the original direct-to-Supabase anon-key path, unchanged. Only reads of `stylist_report`, `store_goals`, `store_managers`, `milestone_goals`, `reviews`, and the `daily_history_*`/`weekly_history_*` chunks (+ legacy singular forms) are now server-filtered.

**How it works**:
- Two new Supabase tables need to be created by the user (SQL below) — `employees` (id, employee_code unique, phone unique, name, role, store_codes text[], **pin_hash** nullable, active, created_at) and `sessions` (opaque token, employee_id, created_at). Both need RLS enabled with **no policies** — that alone locks the public anon key out of them entirely; only a new Supabase **service-role key** (server-only, `SUPABASE_SERVICE_ROLE_KEY` in Vercel, never `REACT_APP_`-prefixed) can touch them. This is the same trick that let this round skip a full data-access rewrite: the existing `weekly_report` table's RLS/anon-key setup is untouched, but the *new* server-enforced endpoint reads it with the service-role key instead of the client reading it directly.
- **Login is a global PIN with no username at all** — this was a deliberate mid-session redesign away from phone+password. "Create a Login" (two-step wizard in `LoginScreen`, `src/App.js`) still checks employee code + phone against the roster the owner uploaded (`api/auth.js`'s `checkEligible` then `signup` actions), but once a PIN is set, "Sign In" is just the PIN — nothing else. **Consequence**: since there's no username to look an account up by, the PIN itself has to double as the identifier. `findEmployeeByPin` (`src/serverAuth.js`) finds the account by bcrypt-comparing the entered PIN against *every* active, registered employee's hash in turn (a hash can't be looked up directly) — used both by login and by signup (to reject a PIN someone else already has, since two people can't share one). This is deliberately O(n) in employee count and uses a lower bcrypt cost (8, not the usual 10) so that scan stays fast as the roster grows — fine at this franchise's scale, would need rethinking if the roster ever reached the thousands. PINs are validated as 5+ letters/numbers (`PIN_PATTERN` in both `LoginScreen` and `api/auth.js`).
- `api/auth.js` (checkEligible/signup/login/logout), `api/roster.js` (owner-only: list/upload/resetPin), `api/scoped-data.js` (the filtering endpoint — role `owner` gets everything unfiltered, everyone else gets their `store_codes` intersected against the payload). Sessions are opaque random tokens in the `sessions` table, not JWTs — every request re-reads the employee's *current* row fresh, so revoking someone (or changing their role/stores) takes effect on their very next request, not just their next login.
- **Setup > Employee Access** (new sub-tab, owner-only since the whole Setup tab is now hidden from non-owners) uploads the roster: one Excel/CSV with columns `Employee Name | Employee Code | Phone Number | Role | Store Codes` (Role: Owner/District Leader/Manager/Employee; Store Codes: comma/space-separated, only meaningful for DL and Manager rows). **Every upload replaces the full current list** — anyone whose employee code isn't in the new file gets `active = false`, which is what actually implements "remove someone from the list, they lose access." Parser: `parseEmployeeAccessFromGrid`/`parseEmployeeAccessFile` in `src/parser.js`. A "Reset PIN" button per row clears `pin_hash` so someone locked out (or who forgot their PIN — there's no self-service recovery) can create a new one.
- `src/App.js`: new `currentUser` state gates the whole app behind `LoginScreen` if there's no valid session in `localStorage`. Once logged in, the sensitive keys load via `loadScoped`/`loadScopedByPrefix` (`src/auth.js`) instead of `loadData`/`loadDataByPrefix` (`src/db.js`) — same `{data, source, error}` shape, so no call-site logic had to change beyond swapping which function is called. `TABS` hides `'Setup'` for non-owners. Tilly (`buildAIContext`/`AIChatWidget`) needed **no changes at all** — she reads the same already-scoped state variables (`report`, `history`, `goals`, `reviews`, etc.), so a DL's Tilly session automatically can't see other stores' numbers either, for free.
- `stylist_report`'s `companyTotals`/`storeCount`/`employeeCount` are recomputed by the filter (reusing `rollup()`, now exported from `parser.js`) after dropping stores outside a role's `store_codes` — otherwise a scoped DL would still see a company-wide total sales figure even with individual stores hidden.
- **30-minute idle auto-logout** (`useIdleLogout` in `src/App.js`) — resets on any mouse/keyboard/touch/scroll activity, not a flat un-renewable session cap; a warning banner shows in the last minute before it fires. Added specifically because login is now a bare PIN with no username on a device that might be shared in the salon — leaving a session open indefinitely was a bigger risk once there's no second factor to also match.

**Not done / deferred**: role `'employee'` can sign up/log in (same roster drives it) but has no real per-person stats view yet — lands on the normal tabs with nothing scoped to them specifically, since "employee sees own stats" was explicitly a someday-maybe in the earlier points/rewards discussion, not this round's ask. No PIN-recovery self-service (owner clears `pin_hash` via "Reset PIN" in Setup > Employee Access, person creates a new one) — no email/SMS infra exists to do it any other way yet. No rate-limiting/lockout on repeated failed PIN attempts — worth adding if this ever feels abusable in practice, not built this round.

**Deployment is done**: `employees`/`sessions` tables created in Supabase (RLS on, no policies), `SUPABASE_SERVICE_ROLE_KEY` set in Vercel, owner row seeded and confirmed working (name "Leadership", role owner). Both Vercel projects (`supercuts-analytics` and `supercuts-analytics-llxh`) are green on the latest commit. The user is actively using the `-llxh` URL day to day — worth remembering that's the one that matters for "is it live" checks, not just the primary project name.

## Post-launch bug — reports appeared to vanish (found and fixed same session)
Right after going live, the user reported stylist_report/history/goals data disappearing on every refresh, with no error shown. Root-caused via the deployed bundle + the user's own browser DevTools (Network/Console) rather than guessing — worth reading if a similar "data disappeared" report ever comes back, since the debugging path (pull the public anon key straight out of the deployed JS bundle to query Supabase directly and confirm the data still existed server-side) is a reusable technique for this kind of "is it a save bug or a read bug" question.

- **Actual cause**: this app already mirrors a lot of data into `localStorage` as a backup (`db.js`'s `saveData` — historical daily/weekly chunks, and especially `homepage_news`, which can carry base64-inlined images/PDFs up to 8MB each). On this user's browser that mirror had grown close to the browser's per-origin storage quota. `setSession()` (`src/auth.js`) writes the login session to `localStorage` too — a tiny write, but with zero headroom left it threw `QuotaExceededError`, which the original code caught and silently ignored. Meanwhile `loadScoped`/`loadScopedByPrefix` were re-reading the token via `getSession()` (i.e. re-reading `localStorage`) on every single fetch instead of trusting the token already held in React state — so with the session never actually persisted, every scoped fetch quietly saw "not logged in" and returned nothing, even within the same already-authenticated page view, no error surfaced anywhere.
- **Fix** (commit after the login-system launch commit): `loadScoped(key, token)`/`loadScopedByPrefix(prefix, token)` now take the token explicitly from `currentUser.token` (App.js) instead of re-reading `localStorage` — matches how `rosterList`/`rosterUpload`/`rosterResetPin` already worked. `setSession()` now returns whether the write actually succeeded; `LoginScreen`'s `onLoggedIn` passes that through to a new `handleLoggedIn` in `App.js`, which shows a toast ("you'll need to sign in again next time") when it didn't, instead of failing silently.
- **Not fixed / still true**: if a browser's localStorage is genuinely full, that browser still won't remember a login across a refresh (harmless now — just means signing in again next visit — but no longer causes data loss). The underlying cause (base64 images/PDFs from Homepage News accumulating in localStorage, per the existing "Known limitations" note further down) is unchanged; worth real attention if this keeps coming up for the user's own browser or anyone else's.

## Still open — real per-role verification pass (DL/Manager scoping)
The roster upload is confirmed successful: user checked Supabase directly (`SELECT role, count(*) ... FROM employees GROUP BY role`) and real DL/Manager/Employee rows are populated, not just the seeded Owner row. Both pending SQL statements (drop phone unique constraint, drop phone NOT NULL) were run. The sequence of blockers from the upload work itself (wrong file → phone-uniqueness failure → phone NOT-NULL failure → the self-lockout bug, all described above) is now fully resolved — no need to revisit that.
- **Not yet done**: the real verification pass from the original plan — sign up as a real DL and a real Manager (private/incognito window, using their real employee code + phone from the roster) and confirm each only sees their own store(s) across Stores/Employees/DL/Reviews/Weekly and via Tilly; confirm removing someone from a re-uploaded roster cuts off their access without needing them to sign out first.
- **Known residual data-quality issue, not fixed automatically**: the Master Salon List workbook itself lists two different phone numbers, in different tabs, for at least 4 people (Katie Ingram, Megan Friday, Shawna Bate, Tricia Hislop/Hilsop) — the user picked which number was correct for each during CSV-building, but the automated Master List parser has no way to know that preference and will just use whatever the file says. If either of these people can't sign up, check their phone in Setup > Employee Access and fix it inline (the new per-row edit feature) rather than assuming the roster data is broken.
- `EmployeeAccessRoster.csv` (`C:\Users\evanr\Downloads`, not in this repo) is fully filled in and still works as a fallback upload if the Master List path has problems, but the user has moved to uploading the Master List workbook directly.

## What shipped this session — Signature S metric
The user wanted a new $ metric, "Signature S," tracked everywhere store/employee data shows up. Turned out to only be derivable from one specific file, which shaped the whole implementation:
- **Source of truth is the Sales-Accrual export only** (Setup > Historical Import), matched by Item Name — `isSignatureServiceItem()` in `src/parser.js` matches `/signature\s*service/i`, confirmed against the user's actual item-name text ("Signature Service"). The routine weekly Stylist Report has no item-level detail at all and can never carry this figure — same situation Gift Cards was already in, so this follows that existing precedent rather than inventing a new one.
- **Full pipeline, store- and employee-level**, mirroring how Color Sales already flows through: `parseSalesAccrualFile` tracks `signatureS` per store-day and per employee (attributed via the `Stylist` column, same as Color) → `mergeSalesIntoHistory` persists it into permanent `history` → `EMPTY_RANGE_TOTALS`/`addRangeInto`/`historyTotalsToReportShape` (date-range queries used by Stores/Retail/Color Sales/DL tabs) → `EMPTY_TOTALS`/`addInto`/`periodTotals` (the separate pipeline behind the Weekly tab and Tilly's monthly history — yes, two parallel aggregation pipelines already existed in this codebase before this change, both needed the same field added).
- **Visible everywhere Color Sales/Retail already are**: added to `STORE_METRICS`/`EMPLOYEE_METRICS` (Employees tab table + sort dropdown + Top10/Bottom10 leaderboards, all automatic from those arrays), `WEEKLY_METRICS` (Weekly tab), the hand-coded `dl-card-stats` chips and `EmployeeTable` `footer` objects in `StoresTab`/`StoreMetricTab`/`DLTab`, and DL tab's fully hand-coded `<table>` columns in **both** its Managers-view and Full-Stats-view variants (header + body + footer cells; bumped the Full-Stats view's employee-expand `colSpan` from 13 to 14 to match). Also added to Tilly's `buildAIContext` monthly company-wide and per-store history lines.
- **Deliberately NOT added**: no new dedicated "Signature Sales" tab (unlike Retail/Color Sales, which get their own DL-grouped `StoreMetricTab` with goal-tracking) — the ask was visibility, not a whole new goal-tracked leaderboard; easy to add later via the same `StoreMetricTab` component if wanted. No Homepage Top 10 leaderboard slot either (would be a 5th leaderboard, a layout decision not asked for). No employee ranking for it in Tilly's "TOP EMPLOYEES BY MONTH" lines (`topEmployeeLine` stays hardcoded to Sales/Retail/Color, matching its existing scope) — the raw $ figures are in context, just not a top-5 ranking on this dimension.
- **Important architecture note the user is aware of**: "current period" (the live weekly Stylist Report, `report` state) and "historical range" (`history`/`weeklyHistory`, fed by Sales-Accrual imports) are two separate view modes in this app — Signature S only ever comes from the historical/Sales-Accrual side, so it always reads $0 on the default "current period" view of Stores/Employees/DL. To see a real number, the user needs to pick a date range (via each tab's date-range picker) that covers days actually covered by a Sales-Accrual upload. The user said Sales-Accrual will be uploaded **daily** going forward (not just as a one-time historical backfill, despite the tab still being labeled "Historical Import") — once that's routine, selecting "today" (or any recent range) in the date picker will show current Signature S numbers same as any other historical metric.
- **Not yet done by the user**: actually uploading Sales-Accrual data — for now they're backfilling history; daily uploads start "in the future" per their own words.

## What shipped last session — auto-upload reports from Gmail
The user wanted weekly report emails to upload themselves instead of manual drag-and-drop into Setup > Upload. No domain, no paid inbox-parsing service — built entirely on things the user already has for free (Gmail + Google Apps Script + Vercel).
- **`src/parser.js` refactored, not rewritten**: `parseStylistReport`/`parseEmployeeStartDates`/`parseReviews` (the `File`-taking, `FileReader`-based browser versions) are now thin wrappers around new exported `parseStylistReportFromGrid`/`parseEmployeeStartDatesFromGrid`/`parseReviewsFromGrid(grid, fileName)` — same logic, just grid-in instead of File-in, so it also runs in a Node serverless function with no FileReader/File available. `sheetToGrid` is now exported too. `buildWeeklyRecord`/`mergeWeeklyIntoHistory` were already pure functions, reused as-is. **Browser upload behavior is unchanged** — this was pure code motion, not a logic change.
- **New `api/email-report.js`** (Vercel serverless function): POST endpoint taking `{ secret, type, fileName, contentBase64 }`. Checks `secret` against the `EMAIL_INGEST_SECRET` env var (this endpoint has no user session, so a shared secret is the only gate — treat it like a password), decodes the base64 attachment with `XLSX.read(buf, {type:'buffer'})`, runs it through the matching `...FromGrid` parser, and saves via a direct Supabase call (not `src/db.js`'s `saveData`, which unconditionally touches `localStorage` — not available in a Node runtime). For `stylist_report`, also replicates the weekly-history merge `handleFile` does in `App.js`, but scoped to just the one affected month's chunk (load `weekly_history_<month>`, merge, save back) instead of the browser's full in-memory history — same result, no need to pull every month into memory. Add a new report type by adding one key to the `HANDLERS` map here.
- **New Setup > Email Reports tab** (`EmailReportsSetupTab` in `App.js`) is the in-app walkthrough — mirrors the existing "One-time Supabase setup" card pattern already in Setup > Guide. Shows the full Google Apps Script source (with the real webhook URL auto-filled via `window.location.origin`, but `SHARED_SECRET` always shown as a placeholder — **the real secret is never embedded in the deployed bundle**, since the Setup tab has no password gate and the bundle is public). The script: watches 3 Gmail labels (`SC-StylistReport`, `SC-Reviews`, `SC-EmployeeStartDates`) via a time-driven trigger the user sets up manually (every 30 min), forwards any unread labeled email's attachment to the webhook, marks it read on success, and emails the user directly on failure (leaving the source email unread so it retries next run).
- **Infra side is fully set up as of this handoff**: the Apps Script project ("Supercuts Report Forwarder") is pasted in with the real shared secret, authorized (one-time "unverified app" click-through), running on a 30-minute time-driven trigger calling `checkForReports`, and Vercel's `EMAIL_INGEST_SECRET` env var is set to match the script's `SHARED_SECRET` (the value was generated once and given to the user directly in chat — deliberately never written to this repo; rotate by changing it in both the Apps Script project and Vercel's env vars if it's ever exposed). **Still pending**: the actual Gmail filter + label. The Stylist Report currently arrives as a manual export (no email yet); the user is expecting an automated/corporate system to start emailing it at some point, but the sender address isn't known yet. Once that first email actually lands, create a Gmail filter (From: that sender → apply label `SC-StylistReport`, don't mark as read) — the script is already watching for that label, so nothing else needs to change once the label exists. Reviews and Employee Start Dates aren't part of this yet (no email source identified for those either) — same drill (a label + Gmail filter) whenever/if that becomes relevant, no code changes needed since `api/email-report.js` already handles all three types.
- **Known limitation**: request body size. Vercel serverless functions cap request bodies around ~4.5MB; a base64-encoded attachment runs ~33% larger than the original file. Fine for a normal multi-store stylist-report Excel export, but if the export ever grows much larger (many more stores, or a much wider column set), this endpoint would need chunking or a move to direct blob upload — not needed today.
- **Not done / deliberately skipped**: no automatic Mailparser/Zapier-style inbox service (the user chose the free Gmail+Apps Script route over a ~$15-20/mo paid inbox-forwarding service); no review/approval queue before publish (the user explicitly wants immediate auto-publish, same as manual upload today — a bad emailed file fails the same way a bad manual upload would, just with an email alert since there's no one watching the Setup tab when it happens).

## What shipped this session — News tile grid, group manager, fun headers
Third round, same day, directly addressing the "not done" list from the round below: a real group manager, much smaller News tiles, and bigger/more-fun group headers.
- **News tab is now a grid of small tiles, not a stack of full-width post blocks.** `NewsTab` renders each post as a `HomepageMediaCard` in a new `compact` mode (`.homepage-featured-card--compact`, `.news-tile-grid` — 4-up on desktop, down to 1-up on phones) — roughly a quarter the footprint of the old single-column `.news-post` block it replaces. A tile shows only image/title/date/PDF-badge; description and the "tap to read" hint are dropped from the compact style entirely to keep it a clean thumbnail.
- **Tapping a tile opens the full post in `NewsPostModal`** (new component) — a centered overlay with the full header image, body text, and PDF iframe/download link, reusing the same `.news-post-*` classes the old inline block used. A Homepage News card tap still sets `tab='News'` + `openNews={id}`; `NewsTab` now just opens that post's modal directly instead of scrolling/highlighting a big block — simpler, and works with tiles too small to usefully scroll-to.
- **Real group management**, replacing the old "just a string on a post" version: a new `homepage_news_groups` Supabase key stores `[{name, color}]` in **App** state `newsGroups`, and `NewsGroupManager` (new component, top of `HomepageAdminTab`) lets you rename a group (cascades to every post with the old name), recolor it, reorder it with ↑/↓, or delete it (ungroups its posts — never deletes them). A group typed fresh into the News composer's field auto-registers itself into `newsGroups` via `ensureNewsGroup` (called from `handleAddNews`/`handleUpdateNews`), so there's no separate "create group" step. On load, any group name found on a post but missing from `newsGroups` (e.g. from before the manager existed) self-heals into the list (`NEWS_GROUP_PALETTE` cycling for its auto-assigned color) so nothing typed pre-manager silently stopped being manageable.
- **Group headers are much bigger and more decorative** — `.news-group-label` went from 15px to 34px Barlow Condensed, each one tinted by its group's color (via a `--group-color` CSS custom property set inline) with a fading gradient bar trailing off to the right, plus a cycling emoji (`NEWS_GROUP_EMOJI`, picked by the group's position in the manager's order) for personality. The News tab's bucket order is now the **manager's curated order** (not most-recent-post), with the ungrouped bucket always first and header-less.
- **Not done / deliberately skipped:** no drag-to-reorder (↑/↓ buttons only); no group icon picker (emoji is auto-cycled by position, not chosen); still no automatic contrast check on a group's chosen color against its white header text.

## What shipped earlier this session — edit-in-place, event date ranges/colors, first News-groups pass
Second round, same day, before the tile-grid/group-manager rework above superseded parts of it:
- **News posts and Events can be edited in place**, not just deleted and re-added. `NewsComposer`/`EventComposer` both take an `initial` item (or null for a fresh post) plus `onSubmit`/`onCancel`; `HomepageAdminTab` tracks `editingNewsId`/`editingEventId` and remounts the composer via `key={editingId || 'new'}` when the target changes — that remount is what resets the form fields, no manual sync effect needed. Admin rows got a ✎ button next to the existing ✕.
- **Events support a date range and a calendar color.** `EventComposer` gained a Start/End date pair (`endDate` optional — a bare single day just leaves it blank) and an `<input type="color">` (`DEFAULT_EVENT_COLOR = '#1F3A63'`, matching the old hardcoded pill color). `EventsCalendar`'s `eventsByDay` walks every day of the displayed month and checks range overlap via ISO-string comparison, so a multi-day event shows a pill on every day it spans, each pill tinted with `ev.color`. `FeaturedEvents` treats an event as "upcoming" until its *last* day passes (not just its start) and shows "Happening now 🎉" for an event in progress.
- **News posts gained a group field** (first pass — free-text with a `<datalist>` for autocomplete, no management yet; superseded by the real `NewsGroupManager` in the round above).
- Tilly's context includes each event's end date (`"date to endDate"`) and a news post's group tag.

## What shipped earlier this session — the News tab
Added a dedicated long-format **News** tab, on top of the News & Updates feature already on the Homepage from last session:
- **News posts can now attach a PDF** — `NewsComposer` (Setup → Homepage) gained a `PdfUploadField` next to the existing header-image field. `readPdfAsDataURL` (`App.js`) validates type/extension and caps size at `NEWS_PDF_MAX_MB` (8MB) before base64-encoding inline into the same `homepage_news` jsonb row images already live in — no re-encoding is possible for a PDF the way images get downscaled, so the cap is the only guard against payload bloat.
- **Homepage News cards now navigate instead of opening a lightbox.** `HomepageMediaCard` gained an `onClick` prop that takes priority over the old `onImageClick` (still used by Events, unchanged) — tapping a News card calls `onOpenNews(id)`, which sets `tab='News'` and `openNews={id}` in `App`. Cards with a PDF attachment show a "📄 PDF" badge as a hint before clicking.
- **`NewsTab` (new component in `App.js`)** renders every post in full — full header image, full body text (`white-space: pre-wrap`, no truncation, unlike the 2-line-clamped Homepage teaser), and an inline `<iframe>` PDF viewer plus a `download`-attribute link for the attachment. When arriving via a Homepage card tap, it scrolls to that post and flashes a 2.5s highlight ring (`useEffect` keyed on the `openNews` object — a fresh object reference each click, so re-tapping the same post re-triggers the scroll+highlight even though the id didn't change).
- Wired the PDF-attachment flag into Tilly's context (`buildAIContext`) per standing practice — she now knows a post has a PDF attached, not its contents.
- **Not done / deliberately skipped:** no in-app PDF *editing* (delete + re-add only, matching how News/Events already worked); no OCR/text-extraction of PDF content for Tilly.

## What shipped this session — the Homepage tab
Built from nothing to a fairly rich landing page across several rounds of feedback. Current shape:

- **News & Updates and Events are authored in Setup → Homepage**, not on the Homepage tab itself — mirrors the Goals/Managers pattern (edit in Setup, display everywhere else) so the public-facing landing page can't be edited by accident. `HomepageAdminTab` in `App.js` has a composer + delete-able list for each. Both support an optional **header image**: `ImageUploadField` reads the file, downscales it to ≤960px and re-encodes as a JPEG data URL (`readImageAsDataURL`) before it's stored inline in the Supabase jsonb payload (`homepage_news` / `homepage_events` keys) — this keeps payload size sane since there's no separate blob storage in this app.
- **Events render as a calendar** on the Homepage tab: the 3 soonest upcoming events show as large banner cards (`FeaturedEvents` → `HomepageMediaCard`) above a real month-grid calendar (`EventsCalendar`, prev/next nav, today highlighted, event pills per day).
- **News cards use the same `HomepageMediaCard` component/CSS as the featured events** (`.homepage-card-grid`), specifically so they're sized to match rather than stretching full width — this was a direct user correction mid-session.
- **Click a News or Event card's image to open it full-size** in an in-app lightbox (`ImageLightbox`) — cards crop/shrink the photo via `background-size: cover`, so this is how a flyer/poster's actual text gets read. Deliberately NOT `window.open('data:...')` — modern Chrome blocks top-level navigation to data URLs, so a real in-app overlay was the only reliable option.
- **Top 10 Leaderboards** (Retail, Color Sales, CPH, TSTH) — `TopTenChart`, horizontal bar via `chart.js`/`react-chartjs-2` (already in `package.json`, previously unused). Each has a "⬇ PNG" export button (`chart.toBase64Image()` + a `homepagePngBg` plugin so the export isn't transparent). Lives in the main column below the calendar (moved down from the sidebar per user feedback — a wider column suits a 2-up chart grid better).
- **Sidebar, top to bottom**: "🌟 Shoutouts" (`ReviewSpotlightWidget`) cycles 5-star reviews that name a stylist by name (reuses `detectEmployeeMention` from the Reviews tab), 10s per review, pauses on hover, progress bar. Each review gets tagged with whichever of **Quality / Convenience / Hospitality / Atmosphere** its text matches (`CORE_VALUE_CATEGORIES`, keyword-based — includes generic praise like "great job"/"well done" under Quality, not just explicit skill language). Below it, "🧭 Core Values" (`CoreValuesWidget`) cycles the salon's 4 official values (Serve/Grow/Be a Super Teammate/Commit to Excellence — hardcoded in `CORE_VALUES`, sourced from the internal poster image the user shared) every 20s. Title+subtitle render as one uniform bold-uppercase line (subtitle explicitly inherits the title's font/size/weight — an earlier version styled them differently and the user flagged the mismatch).
- **Bitmoji mascot peeks — Homepage only.** 13 user-supplied bitmoji images live in `src/assets/bitmoji/`, grouped by mood in `src/bitmoji.js` (positive/curious/skeptical/all). `useBitmojiCycler` (in `App.js`) is a **single shared coordinator** — only one mascot is ever visible at a time anywhere on the page, hopping between 6 fixed spots that are each anchored to a specific widget's corner (`BITMOJI_PEEK_CORNERS`) so it never appears at a "random" page position. This was a deliberate rewrite: the first version gave every widget its own independent timer, which could show multiple mascots at once — explicitly not what was wanted.

## Related: DL / Retail / Color Sales progress thermometers
Not Homepage, but touched this session while fixing a Homepage-adjacent complaint:
- DL tab's per-DL summary bar (`.dl-card-stats`) was clipping the milestone %. Fixed by folding the milestone $ figure into the thermometer itself (`MilestoneThermometer` now renders a compact "of $60K" label inside the bar) and dropping the separate "Milestone" stat cell that summary row used to have. The detailed per-store `<table>` below (Goal/Milestone/Progress as 3 separate columns) is untouched — only the cramped card-header summary was consolidated.
- Retail and Color Sales tabs (both render via the shared `StoreMetricTab`) now also show a Progress thermometer in their DL-grouped summary bar, replacing the old plain "vs Goal" $ delta — reuses the existing weekly retail/color goal (`goals` state, distinct from `milestoneGoals`) as the thermometer's single target (no separate goal-tick, since there's only one number here, not a goal+stretch pair like DL's monthly sales milestone).

## Discussed but not built — employee rewards/points system
User asked feasibility questions only this session (explicitly said not to implement); nothing below exists in the code yet. Captured here so a future session doesn't re-derive the same design questions/answers.
- **Ask**: award employees points (admin action), track a running balance, show it per-employee; a "shop" where employees spend points on rewards; a text message sent to the employee when they get shouted out.
- **Points + balance**: straightforward extension of existing patterns — new Supabase table/row for per-employee balance + transaction log, admin award action gated like the existing `GOALS_PASSWORD` pattern (`src/App.js`).
- **Shop**: bigger lift than points — needs an admin-managed reward catalog (name/cost/stock) plus a redemption flow that decrements balance and logs the redemption for the manager to actually fulfill in person (app can't dispense real rewards).
- **Texting on shoutout**: needs a real SMS provider — no SMS infra exists today. User said they're willing to pay, so the plan is **Twilio**, not a free workaround (email-to-SMS carrier gateways were mentioned as a free alternative but are fragile/carrier-dependent — ruled out once the user said budget isn't a constraint).
- **Login system (user asked, wants this)**: recommended approach is **phone number + SMS one-time code**, not a traditional username/password — deliberately chosen because (a) it avoids password-reset support burden for hourly stylists, and (b) it reuses the same Twilio number/integration as the shoutout-texting feature, so login and notifications become one piece of infra instead of two. Supabase (already the backend here) has built-in Phone Auth with Twilio, so this is a config/feature-flip on existing infra, not a new system bolted on.
- **New infrastructure this would require that doesn't exist today**: (1) a real employee identity record (name ↔ phone number ↔ point balance) — currently employees are just name-strings matched via the `Stylist` column in parsed reports, there's no employee table/id anywhere; (2) real persisted login sessions — every "password gate" in the app today (Goals, etc.) is just a client-side unlocked flag that resets on reload, not actual auth.
- **Open design question, unresolved**: shared salon device vs. employee's personal phone for checking points — affects whether the login needs an explicit logout/switch-user step (shared device) or can just stay signed in (personal phone). Needs an answer before building the login flow.
- **Not started**: no schema, no Twilio account, no code. Pure design discussion.

## Known limitations (deliberate, not oversights)
- Tilly's historical data is **month-level only** — no day/week granularity in her context.
- Review text sent to Tilly is capped at 40 most-recent-negative / 15 most-recent-positive — aggregates/counts are uncapped, only raw quotes are bounded.
- Manager name matching (Setup > Managers) is exact-normalized-name, not fuzzy.
- Homepage News/Events admin (Setup > Homepage) has no password gate — anyone who can reach Setup can post/delete. Same posture as Goals/Managers before this session; add a gate the same way `GOALS_PASSWORD` does it in `App.js` if that's ever a problem.
- News PDFs are base64-inlined into the single `homepage_news` Supabase row (same pattern as header images, capped at 8MB each via `NEWS_PDF_MAX_MB`) — every save/load moves the *whole* news array, so a handful of large PDFs accumulating over time will make that row (and every News edit) progressively heavier. Fine for occasional flyers/memos; if PDF posting becomes frequent, worth moving to real blob storage (Supabase Storage) instead of jsonb.
- No test suite, and **no Node.js/npm in this sandboxed environment** — every change this session was verified by careful re-reading plus a `perl`-based brace/paren balance check, never an actual local build. **After every push, poll `gh api repos/evanrobins6789-cyber/supercuts-analytics/commits/<sha>/status --jq '.statuses[]'`** for the real GitHub↔Vercel compile check — this was done after every commit this session and everything came back green, but it's the only real verification that exists.

## Architecture notes for a fresh session
- `src/App.js` is one large file (~4300+ lines now) — most tabs/widgets are components within it. Read the relevant function directly rather than assuming structure.
- `src/bitmoji.js` + `src/assets/bitmoji/*.png` — new this session, see above.
- `src/db.js`: generic Supabase (`weekly_report` table) + localStorage persistence layer. `loadData`/`saveData`/`clearData` and `*ByPrefix` counterparts all return `{ok, error}` (or `{data, source, error}` for loads) — check these, especially cleanup deletes after a format-swap save.
- `src/leaderRoster.js`: hardcoded DL/Area Supervisor → store-code roster (edited + redeployed on change — different from Managers/Homepage News/Events, which are user-editable in-app).
- The `supercuts-analytics/` subdirectory in the repo root is a **stale duplicate**, not the deployed app — ignore it.
- Deploys automatically via Vercel on push to `main`. Two Vercel projects exist for this repo (`supercuts-analytics` and `supercuts-analytics-llxh`) — the status-check poll above reports both; both need to be green.

## What's next
**Most immediate**: see "Open items for next session" at the very top of this doc (2026-08-03) — HSA schedule upload, the Rallio email's first real run, Mullica Hill data, and a live look at everything shipped today.

The original Stylist-Report-optional live test (see "Live test in progress" / rounds 1 and 2 further down, 2026-07-30) is long since resolved — two real bugs were found and fixed same-day, and the app has since moved on to defaulting to month-to-date (2026-08-03) instead of week-to-date, so treat any mention of "week-to-date" below this point as historical, not current behavior.

Also still open from 2026-07-28: confirm whether the 3 points/rewards `CREATE TABLE` statements (see "What shipped this session — Employee points" above) have been run in Supabase — if not, walk through them, then do the smoke test described there. Also still open: the DL/Manager scoping verification pass (see "Still open" above) — sign up as a real DL and a real Manager and confirm each only sees their own store(s).

After that: the employee points/rewards system described below (points + shop + Twilio texting) — note the **login/identity piece it would have needed is now built** (real employee records with phone numbers, real sessions, real roles) — re-read that section with this in mind next time it comes up, its "doesn't exist today" framing is now stale. User was engaged on it and willing to pay for Twilio, but hadn't given a go-ahead to implement, and the shared-device-vs-personal-phone question is still open. Confirm that before starting.

Other natural follow-ups if asked: a password gate for the Homepage admin section, drag-to-reorder for groups (currently ↑/↓ buttons only), or more Top 10 widgets (Net Sales, Cuts) if four isn't enough.
